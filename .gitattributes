# Unified diffs require a leading context space, including before source tabs and empty lines.
# Keep their exact bytes; source files retain the normal whitespace checks.
patches/*.patch text eol=lf whitespace=-blank-at-eol,-space-before-tab
