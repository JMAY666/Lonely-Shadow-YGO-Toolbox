# 项目协作要求

本文件适用于当前项目及其子目录。修改本文件时保留已有且仍适用的要求，将新增约定合并进来，不覆盖无关规则。

## 1. 仓库状态与项目边界

- 开始 Git 操作前检查当前路径、仓库根目录、分支、工作区和暂存区状态，以及现有远程与历史。
- 若项目已有 Git 仓库，沿用现有配置和历史，不重新初始化或覆盖历史。
- 若当前目录属于上级仓库，先确认项目边界及管理方式，避免误建嵌套仓库；边界无法确定时先向用户确认。
- 只有确认当前项目尚未纳入 Git 管理且不属于上级仓库后，才初始化独立仓库。

## 2. 排除规则与文件保留

- 按实际目录和技术栈维护 `.gitignore`，排除依赖目录、缓存、日志、临时文件、可重新生成的构建产物，以及密钥、凭据和个人数据。
- 排除仅指不纳入版本管理，不得因此删除本地文件。不要为了清理 Git 范围删除用户数据或运行所需文件。
- 保留运行、开发和复现所需的源码、必要资源、构建配置、数据库迁移、脱敏样例、配置模板和依赖锁定文件。
- 配置模板只能包含占位内容。即使文件名属于模板或未被忽略，也必须审核其内容，不得提交真实密钥、访问令牌、密码、会话信息或个人数据。
- 本地个人数据、账号状态、牌组或对局记录及备份优先放入 `.local/`、`private/`、`local-data/`、`user-data/`、`backups/` 或 `data/local/`。其他路径中的文件仍需单独审核，不能认为忽略规则已经覆盖所有敏感信息。
- 不要笼统排除所有数据库、JSON、图片或 `data/` 目录；公开卡牌资源、测试样例及运行必需数据应根据用途和内容逐项判断。
- 当前仓库包含 Electron 桌面封装、Python 服务、网页界面和原生客户端适配。引入新的技术栈或运行数据时重新检查，防止漏排敏感文件或误排必需文件。

## 3. 审核、检查与本地提交

- 新仓库审核待提交文件及内容后创建初始提交；已有仓库按本次实际变更提交，不创建空提交。
- 以后每完成一项完整修改并通过相关检查后，创建一次本地提交，提交信息清楚说明修改内容。
- 提交前检查 `git status`、未暂存差异、暂存差异及新增文件的完整内容；确认每个文件与本次工作相关且经过审核。
- 使用明确路径暂存文件，禁止使用 `git add .`、`git add -A` 或 `git commit -a` 将无关内容一并纳入提交。对同一文件中的无关改动按范围处理，不覆盖或撤销用户已有修改。
- 按变更运行适用的测试、静态检查或构建。只有文档、Git 管理文件且没有应用源码时，检查内容、链接、忽略规则和暂存差异即可，不虚构代码测试结果。
- 提交前运行 `git diff --cached --check`，再次审核 `git diff --cached --name-status` 和 `git diff --cached`。发现问题先修复并重新审核，再提交。
- 提交后核实实际提交内容和工作区状态；无关修改可以保留在工作区，但必须说明，不能为获得干净状态而擅自删除。

## 4. 推送授权与完整范围审核

- 用户已明确授权本项目后续升级在审核通过后自动推送。每项升级完成后，按本文件完成代码审核、相关检查、README 核对和本地提交，再复核完整待推送范围；审核通过即推送到经核实的现有远程及目标分支，无需逐次再次询问推送许可。
- 本地提交和推送仍是独立步骤；持续授权不能替代隐私、必要文件和全部待推送历史的审核。审核或检查未通过时先修复并重新审核，问题未解决前不得推送；更换远程、额外分支或标签、重写历史和强制推送不属于此次持续授权范围。
- 推送前核实实际远程地址、目标仓库、目标分支、本地分支及将上传的全部提交。必要时更新远程引用，不能仅凭过期的本地引用判断范围。
- 对已有目标分支，从已核实的远程状态确定完整待推送范围，逐项审核提交及其文件内容；目标分支或仓库首次推送时，审核该分支将带入的全部可达历史。
- 审核所有将上传提交中的隐私、敏感信息及不必要文件，包括中途加入后又删除的内容；不能只检查最新一次修改、最终文件树或最后一次提交。
- 若同时上传其他分支或标签，也要审核其引入的全部历史；仅推送已授权且已审核的引用，避免无必要的全量分支或标签推送。
- 如发现历史或文件问题，先处理并重新审核；审核未通过前不得推送。未经核实不得修改远程地址、推送到其他仓库或声称推送成功。

## 5. 已跟踪文件与历史问题

- `.gitignore` 不会自动移除已跟踪文件，也不会清除历史内容。既要检查当前跟踪范围，也要检查本次拟上传的历史。
- 停止跟踪时，在确认范围和本地文件保留方式后使用 `git rm --cached -- <明确路径>` 等仅修改索引的方式；保留本地仍需使用的文件，再审核暂存变更。
- 删除最新版本中的敏感文件不能解决历史泄露问题。发现真实凭据时不在输出中复述其值，并告知需要撤销或轮换相关凭据。
- 涉及重写已有历史、清除历史对象或强制推送等操作时，先说明涉及的提交、分支、协作者影响和恢复办法，获得用户明确确认后再执行。
- 不得擅自进行破坏性重置、清理本地数据或丢弃用户修改。问题未解决时明确报告阻塞范围，不把未完成的处理称为已完成。

## 6. README 与 GitHub 仓库简介

- 每次完整修改都根据实际更新检查 `README.md`。涉及功能、安装、启动、配置或使用方式变化时，同步更新相关说明。
- 文档应与相关代码一起审核、提交，并在获准推送时一起推送，不等推送后再补写。无需为无关的小改动制造 README 修改。
- 说明必须与实际实现和验证结果一致；区分已实现、已验证和计划中的内容，不宣称尚未实现或验证的功能。
- 推送成功后检查 GitHub 仓库简介（About）。若简介已不准确，同步更新为经核实的项目描述；若仍准确则保持不变，无需为无关的小改动反复修改。
- 无远程、未推送、目标不在 GitHub 或没有权限检查/更新简介时，如实报告未执行或未验证的原因。

## 7. 结果报告

完成后简要说明：

- 排除规则及本地文件保留情况。
- 实际执行的检查及结果，注明失败、未执行或未验证事项。
- 本次提交哈希、提交说明与提交后状态。
- 是否推送；若已推送，说明核实的远程和目标分支；若未推送，说明本地分支及原因。
- README 是否检查或更新，GitHub About 是否检查或更新及必要原因。

## 8. 桌面调试与验收

- 使用后台 Electron 和引擎内部测试接口进行验收，运行 `npm run test:desktop` / `npm run test:packaged`；不移动系统鼠标、不发送全局按键、不接管用户正在使用的其他窗口。
- 截图从本应用的渲染器或原生渲染驱动获取，测试只操作本应用创建的窗口与隔离测试数据。原生场地通过子窗口嵌入主界面，保留现有规则引擎和记录格式。
- 内部输入／截图接口仅在测试启动参数下启用，正常启动默认关闭。测试随机种子仅限明确标记的验收实例，不能改变正常训练的随机起手。
