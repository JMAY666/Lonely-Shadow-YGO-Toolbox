# Master Duel 适配验证（2026-09-18）

新增第四个平台入口和独立只读适配器，沿用现有智能识别、TAG、冻结起手及跨局监听；不开放编辑器卡组识别。真人六轮的先后攻、起手核对和边界见 [适配说明](masterduel-recognition.md)。

共享工作区同时有其他功能修改，因此本次从 `94aa7a1` 建立独立检出，只复制 Master Duel 相关文件、必要公开资源与文档用于验收。其他修改和旧应用包保留，不纳入本次提交；独立验收包沿用基线 1.28.1 版本号，存放在本地 `release/masterduel-validation-1.28.1/`，不是共享工作区其他升级的发行包。

| 检查 | 结果 |
| --- | --- |
| 新适配器定向合成测试 | 9 项通过；覆盖 Solo／PvP 两套布局、我方 0／1 号映射、先后攻、重复起手、晚接入、数据变化、未知卡、未适配构建、进程身份变化以及拒绝对方／账号值读取 |
| 捕捉与智能识别回归 | 捕捉 32 项、智能识别 19 项、起手 9 项通过 |
| 共享工作区 `npm test` | Python 374 项、Node 203 项通过，仅作为当时共享工作区的集成结果 |
| 独立检出 `npm test` | Python 369 项、Node 200 项通过 |
| `npm run test:desktop -- --smart-only` | 独立检出通过：四平台识别交互、取消／重试、迟到响应、冻结起手、方案调用与持续跨局 |
| `npm run test:packaged -- --smart-only` | 独立验收包通过相同四平台专项；无需开发 Python 或额外运行时依赖 |
| `npm run test:packaged -- --automatic-only` | 通过原 YGOPro 逐步流程：卡组读取与失败门控、保存／确认覆盖、TAG、先后攻、起手预览和跨局清理 |
| 应用资源核对 | 打包成功；111 个打包应用资源与独立检出源码逐一一致，包含卡号映射、许可证及平台 SVG |
| 六轮真人联调 | 先攻／后攻、两套构筑、重复卡、同一连接连续下一局、后续抽牌不覆盖快照均已核对；正式先攻到达方案页 |
| 平台视觉 | 四平台图标离线可用，900／1280／1600 宽度等宽方形、桌面并排；截图已查看 |

首次共享工作区桌面专项在新平台的标题断言处失败：测试使用进程名 `masterduel` 匹配正式显示名 `Yu-Gi-Oh! Master Duel`。已修正断言并增加投硬币文案检查。原逐步流程的测试平台计数从 3 更新为 4，最终复测通过。独立检出首次启动缺少本地嵌入 Python 运行包，在挂接已有的同版本只读运行资源后重新运行；失败启动不计为桌面验收通过。

真人采样结束后游戏已关闭，因此没有再用打包 Python 执行额外真人对局采样；打包版以相同读取器字节核对和后台桌面专项为证据。没有执行完整桌面大基线，不将专项通过等同于全功能验收。

个人构筑、起手、进程信息、元数据、截图、时间线、研究脚本和 ZIP 仅留在忽略目录；纳入 Git 的映射表来自公开来源，图标取自客户端资源，合成测试不包含真实卡组。全程只驱动本应用的后台隔离窗口，不移动系统鼠标、不发送全局按键。
