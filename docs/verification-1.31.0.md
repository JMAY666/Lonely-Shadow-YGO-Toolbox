# 1.31.0 验证记录

## 实现范围

P1.3 的有限内置练习接入：读取相同构筑及起手的后攻主线，在隔离引擎中完整恢复当前规则历史；复用正式与妥协来源，按公开怪兽处理目标搜索未执行后续。具体限制见[操作说明](bo1-second-routes.md)。

## 检查结果

- `npm test`：最终 417 项 Python、216 项 JavaScript 测试通过，包含原先攻、后攻观察及条件提示的回归。
- `npm run test:modular -- --suite=second-routes` 与 `npm run test:modular:packaged -- --suite=second-routes` 通过：原局日志不变、冻结起手不变、公开信息过滤、清除公开怪兽并建立连接终场、实际改为先通召后重新续算、通召次数已消耗、对手真实灰流丽响应后从实际结果续算、我方真实手坑消耗加正常抽牌后仍为五张。
- 上述开发／打包版专项包含真实服务的界面检查：条件说明、来源操作列表、计划不扣资源、人工修改使候选停止采用、900px 窄窗口无横向溢出。打包版截图核对路线详情；区域名称和空响应自动确认的文字随后由 JavaScript 定向测试覆盖。
- `npm run test:desktop -- --second-only` 与 `npm run test:packaged -- --second-only` 通过，原 P1.1/P1.2 的手牌、历史恢复、先攻入口、三种提示与实际发动结果功能保留。
- `npm run test:modular -- --suite=forecast` 通过，原先攻抽牌、堆墓、实际交灰后续算，以及另一真实展开的输入日志和生命周期隔离保持正常。本轮没有重复运行全部既有桌面专项。
- 审核补充的决策节点格式拒绝、同一节点刷新新增来源、失败写入不发布候选和迟到结果拒绝，均由最终单元测试覆盖。

## 发布检查

使用已核实的离线原生资源构建 1.31.0 目录包和 ZIP。最终包核对应用版本、源码／目录包／ZIP 中程序一致、规则资源一致及个人记录排除；详情保存在 `.local/evidence/bo1-p13-package-check.json`。原生核心未改动，既有 P1.2 有限案例资源指纹继续适用。

源码、文档与合成测试纳入版本管理；原生测试局、程序包、截图及原始日志继续保留本地。提交前按精确路径、进程、服务锁及不可变资源清单核对可重建的测试副本，清理记录保存在 `.local/evidence/bo1-p13-cleanup-result.json`；设置、备份、JSON／JSONL、正式证据和未知文件保留。

## 未覆盖

外部平台完整状态、MD 卡池与禁限表、多卡组全面交互、主动枚举对手响应分支、完整威胁排序及战斗伤害／斩杀仍未验收。关联前已有独立人工观察时拒绝覆盖；P1.2 人工交康记录与原生接入的统一衔接留待 P1.4。第一期整体尚未完成。

检查只使用本应用创建的隐藏 Electron、内部规则接口和隔离测试数据，没有操作用户的其他窗口、系统鼠标或全局按键。原始证据、测试记录和安装包保留本地。
