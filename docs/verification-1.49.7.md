# 1.49.7 统一批次首阶段验收

日期：2026-09-26。冻结的No.／英雄／银河386张剩余卡清单保留不变。本阶段处理前100张，81张经逐卡核对核准合并，19张具体来源、模型或规则问题暂缓；累计2,569／14,716，尚余12,147。该阶段不是386张或三个系列全部完成声明。

为了验证本阶段新增的共用动作、次数、动态数值和查询门禁，已处理条目形成独立发布阶段；每20张检查点已保存。剩余286张仍待处理，19张暂缓不升`reviewed`，详见[完整阶段清单](card-annotation-batch-2026-09-26-fast-series-stage01.json)。

## 规则核对与整合

- 额外卡组确认、从对方额外出场和从自己额外出场分别保存；不把主卡组翻开与额外确认混用。
- No.9攻击无效每战斗步骤一次、不取除素材为费用；No.18每连锁一次；否定人②同名决斗一次。魔性人没有同名一次，银河修道师两效果同名共池一次，均不套用相同默认值。
- 取除或解放作为处理和费用分开：CNo.79②处理取除材料，光子咆哮②解放是处理；固定获赋效果的条件、TAG、费用和类别保持独立。
- 神杖先驱①保留强制发动与无合格墓地卡时0对象的补足，补齐本地旧文省略的フュージョン魔法类别。黏土守卫侠①额外自锁仅从处理后适用，不能拿中文“这个回合”自动判断回溯。
- 动态伤害／回复、当前数值和原本数值依据明确保存；支付一半LP不是造成伤害，伤害替代不是本卡直接烧血，贯通仍为战斗伤害。
- 电流侠原包错配为另一只融合怪兽、光波龙错配为光波刃龙，均使用独立纠错来源核实，不改写原包。No.101新卡与F・HERO的本地双身份保留整数卡号，单独核对来源。
- 19张暂缓保留具体理由。未编号段混合多个独立规则／效果时不虚构编号或获赋；源卡文与补足冲突时不挑选有利句子来通过。

## 实际检查

- 阶段全量门禁：81张、168条真实查询、11条规则字段断言、3条用途边界断言及368个来源文件SHA-256通过。未知查询字段被拒绝，效果类别等无对应搜索条件的事实使用独立断言，不宣称不存在的筛选已验证。
- 当前runtime的冻结卡文、分段、词表和引用：2,569条通过；20系列／35样例通过。
- 独立于生成器的原始卡页身份机械重验：81张KONAMI日文原始HTML及类型／属性／数值重验通过；仅未来龙皇的卡面0阶与实际1阶产生预期差异，官方明确非效果规则按1阶处理。身份与数值一致不代替逐卡语义审查。
- 旧2,488条规范记录与合并前备份逐项完全相等；实际新增81条，未改写真实个人运行资料。
- 完整`npm test`通过：683项Python、256项Node。新增额外区域正反例、动态数值互斥／时点／非执行字段、次数范围、追加属性与未知查询参数门禁回归通过。
- 后台开发版`npm run test:desktop -- --annotations-only`通过；新构建`npm run build:dir -- --config.directories.output=.local/annotation-pipeline/fast-series-01/package-1497-current`通过。
- 当前1.49.7打包版`npm run test:packaged -- --annotations-only`通过：EXE名称、描述、7帧图标、额外确认、No.9次数／无取除费用、否定人决斗次数、正确电流侠与来源、动态400数值说明、旧个人修正和布局通过。
- 开发及打包均使用独立`YGO_TEST_RUN`、空的隔离导入和后台窗口，标注报告`errors=[]`、`globalInput=false`，未控制系统鼠标或全局键盘。当前文档／说明重新构建至package-1497-current后重新通过打包验收，8个主要应用源码／资料SHA-256与工作区逐项相等。

日志、候选、决策和身份审计位于`.local/annotation-pipeline/fast-series-01/`；完整阶段冻结与合并备份位于同级`fast-series-stage01/`。最终桌面证据目录分别为`.local/evidence/electron-development-annotations-fast-series-stage01-development-final-1497/`和`.local/evidence/electron-packaged-annotations-fast-series-stage01-packaged-current-1497/`。

## 续接、资料与发布

下一项为原任务第101个卡号29085954《No.78 编号系档案馆》。续接读取当前规范库和原386张任务，不重复计81条已合并数据，不将386份机械全文包计为已审核。待核实说明在进一步读纠错来源后更正，原合并时的私有验证快照保留；更正说明不增加核准数量。

原始采集包、纠错包、手写决策、全文、断点、缓存、备份与当前验收包沿用`.local`忽略规则保留本地。清理只限本阶段可重建且已被最终结果替代的中间输出，不删除正式来源、证据或任何用户数据。未恢复封存的AI学习计划。

README、体系规范、交接与进度均按实际范围更新，提交和推送按`AGENTS.md`审核现有远程及全部待推送历史。源码与公开清单入库，本地来源和程序包不上传；提交哈希、工作区和推送结果以最终报告及实际Git记录为准。
