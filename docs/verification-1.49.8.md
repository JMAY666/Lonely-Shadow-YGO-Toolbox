# 1.49.8 卡片续接与数量协议验收

日期：2026-09-26。本阶段从保留的No.／英雄／银河386张任务第101～240项继续，逐卡处理140张，113核准合并、27具体待核实。累计2,682／14,716，尚余12,034。原清单前240项合计194核准、46待核实，后146项尚未处理；下一项为第241项62107981《元素英雄 小花蕾》。不宣称三个系列或全库已完成。[逐卡清单](card-annotation-batch-2026-09-26-fast-series-stage02.json)保留全部接纳／暂缓原因。

## 来源、身份与语义

- 逐卡阅读所引日本KONAMI OCG卡页、补足及所需FAQ，完整本地卡文、真实分段、费用／对象／处理／类别／时点／次数逐项保存。异画编号独立保留。
- 重铠光子龙附素材的1至2闭区间，天空侠破坏的处理时动态上限，星云新宇侠的动态抽卡与对象数量分别表达。处理数量不替代实时求值或发动合法性。
- No.85掷骰六分支完整保存；LP减半与费用、烧血分开。CNo.104两版身份各自保留，随机送墓成功后的减半与丢弃不同；旧版白煞使用纠错cid10261，原包错配的新Revice不导入。
- 同名适用一次、攻击宣言一次、伤害计算一次与每回合使用一次不同。辉光子龙暂时除外的对方怪兽返回不新开连锁、不算特召；其非连锁出场的解放不是效果解放。
- CNo.62的两个真实固定获赋永续单元分别核对，不把免疫与另一效果的费用拼接。双方抽卡／原持有者检索按每个移动项绑定来源和去向，保留跨玩家组合反例。
- 27条具体暂缓含同段独立效果、延迟预约、强制攻击／骰面替换／战伤转移、缺来源和官方补足冲突，未设`reviewed/manual`。原386项决策与候选继续保留，不用虚构获赋或编号通过。

## 实际检查

- 最终强制全量门禁：113条、229条真实查询、9条语义字段断言、3条用途边界断言，476个引用来源文件SHA-256通过；没有用检查缓存代替最终复查。
- 独立于手写组装器的原始HTML身份／类型／属性／数值重验113张通过。41522092、43490025、52653092卡面0阶与规则1阶产生预期差异，依据官方非效果规则解释；机械身份核验不等于逐卡规则审核。
- 合并前2,569条与备份逐项完全相同，实际新增113条。完整原任务140／160／180／200／220／240项等约20张检查点实际执行；最新断点与指纹保留在本地。未改写真实用户个人运行资料。
- 全库卡文／分段／来源／词表：2,682条通过；20系列／35样例通过。
- 卡片相关Python检查143项通过；标注界面Node检查23项通过。新增对方回场反例初次测试漏限定卡号、把其他样例特召也计入，修正测试过滤范围后通过，未削弱产品检查。
- 完整`npm test`通过：687项Python、257项Node。初次有一项补丁安装测试遇到Windows目录改名`WinError 5`，该补丁测试单独10项及完整重跑通过；产品检查未关闭。
- 终审补齐酸水扫除魔陷的场地区／灵摆区查询，以及16个已在备注核对的具体召唤事件结构；额外3条断言防止通用`on_summon`被理解为所有召唤都满足。之后完整批次门禁、全库检查、143项卡片相关Python和当前源码桌面验收重跑通过。
- 最终后台开发版`npm run test:desktop -- --annotations-only`与新构建`npm run build:dir -- --config.directories.output=.local/annotation-pipeline/fast-series-stage02/package-1498-release`通过。
- 当前1.49.8打包版`npm run test:packaged -- --annotations-only`通过，使用上述包目录：EXE名称、描述、7帧图标、新数量详情、LP减半正反例、三种事件／适用次数、对方暂除返回与旧白煞身份查询通过。详情与能力事实均显示处理数量依据，不执行表达式。
- 开发及打包使用独立`YGO_TEST_RUN`、隔离数据与后台窗口，报告`errors=[]/globalInput=false`。160个打包运行资源的SHA-256与当前`src/trainer`逐项相同，`app.asar`的版本、名称、入口等运行元数据相同；打包器省略`scripts/devDependencies/build`属于预期，不宣称源与包的完整package.json字节相同。

最终桌面证据分别位于`.local/evidence/electron-development-annotations-fast-series-stage02-development-release-1498/`与`.local/evidence/electron-packaged-annotations-fast-series-stage02-packaged-release-1498/`。实际应用截图核对通过；完整检查日志、来源身份、保留对照和打包哈希在本阶段本地目录。

## 续接、数据与发布

本阶段发生共用数量、LP、次数和展示变化，按流程单独完整验收；不因一个20张检查点结束原完整批次。原386张固定任务仍在`.local/annotation-pipeline/fast-series-01/`，本阶段决策、候选、合并备份与正式验收资料在同级`fast-series-stage02/`。原始资料包、纠错包、全文和身份审计仅本地保留。

`.gitignore`沿用现有`.local`、依赖、构建及个人资料排除规则；不删除用户数据、来源、正式证据或备份。3个已被最终包替代的中间构建目录删除被自动审核以`blocked by policy`阻止，仍只本地保留，不入提交。README、体系规范、交接与进度按本次实际结果同步，未恢复封存AI学习计划。按`AGENTS.md`审核明确文件和完整待推送历史后提交、推送现有`origin/main`，并检查GitHub About；最终哈希及远端结果以Git核验为准。
