# 1.36.1 后攻计划回退记录

用户要求暂时回退本任务全部计划实现，同时把研究数据留在本地供后续参考。本次使用新的回退提交，保留既有 Git 历史，不重置或强制推送。

## 撤回范围

撤回 `d8bb204`、`0aa8570`、`fd3c628`、`41a373b`、`9157055`、`1ad5dcd`、`91d27ca`、`e282f5d` 中的后攻计划实现，即 1.29.0–1.36.0：

- 独立后攻局面、当前资源、交康提示及后攻工作区入口。
- 内置后攻规则重建、两阶段续算、窗口复盘和指定攻击验证。
- 外部 YGOPro 公开资源、阶段、回合玩家、素材、连锁和我方选项快照。
- 对应的后攻专用服务、网页资源、正式证明夹具、专项检查和进行中计划文档。

产品功能恢复到 `b0acf30`（1.28.5）的既有流程。`src/trainer` 与该基线逐文件比较，仅保留一行共用定位修正：模块默认名为 `YGOPro.exe`。它服务于原有卡组／开局识别，原误写值会导致固定构建定位失败；相关回归检查保留在通用捕获测试中。没有保留任何后攻状态跟踪或策略能力。

`17af5b7` 的独立 ygo-agent 实验与随机牌检查修正，以及 `aac7526` 的 AI 学习可行性计划不属于本任务回退范围，保持原样。既有原生规则核心、正式方案、先攻流程、开局识别和初始起手逻辑继续保留。没有改写随机起手、客户端输入或其他平台的规则范围。

## 本地保留

归档目录：`.local/archives/bo1-second-paused-aac7526/`。

- `tracked-before-rollback.zip`：回退前完整已跟踪文件快照，含源代码与检查，已验证 ZIP 完整性。
- `patches/`：上述 8 项提交的完整补丁；`research-docs/`：18 份原执行、规则校正、布局及验收文档。
- `manifest.json`：回退前提交、归档与补丁哈希、选定研究／验收资料的原路径和内容校验。
- 现场读取、研究脚本、原始 JSON／JSONL、截图、既有后攻记录及旧发布包原位保留；不删除个人数据。当前产品不读取这些已暂停的后攻记录，后续恢复前仍需核对接口及规则版本。

归档及真人资料继续由既有 `.local/`、`release/` 忽略规则隔离，不上传。新程序包仅带当前有效文档，旧后攻使用说明不继续作为现行功能分发。没有清除远程历史中的旧提交；需要参考旧实现时仍可查看历史。

## 检查

- `npm test`：375 项 Python、207 项 JavaScript 通过。
- `npm run test:desktop -- --duel-only` 与 `npm run test:packaged -- --duel-only` 通过：先后攻监测、五张起手及后攻留存页、卡组捕获合成接口、先攻教程、正式方案保留、分支、快捷键和多种窗口大小均覆盖。
- 开发版智能识别定向检查通过四个平台的合成场景：开局进入先攻、原起手冻结、跨局、断流、迟到结果和后攻“暂未支持”提示；命令为 `npm run test:desktop -- --smart-only --duel-only`。前一个参数选中智能识别专项，后一个参数复用决斗隔离目录并跳过无关的启动标题栏专项；本次没有据此声称两个专项重复执行。
- 首次单独 `--smart-only` 在启动标题栏的后台 `capturePage` 截图处返回 `UnknownVizError`，未进入智能识别检查。原失败 JSON／截图以 `smart-startup-*` 保留；随后以开发版 `--home-only` 执行同一启动标题栏路径，900／1280／1600 宽度、125% 缩放、滚动和首页均通过。复查没有修改产品或截图实现，初次失败仍单独记录。
- 独立 ygo-agent 的 13 项既有单元检查通过；没有运行新的模型训练或扩大其功能。
- 源码与 `b0acf30` 比较，仅有上述共用模块名修正；独立实验及其并行文档更新没有被覆盖。撤下的 60 个文件与归档逐项核对，除 Git ZIP 导出的 CRLF 转换外内容一致。
- 7,371 份选定研究／验收文件逐项比较原清单，未缺失、未改变；归档 ZIP 哈希保持一致。此项证明清单内文件的保留，不冒充全部个人数据的备份证明。
- 目录包／ZIP 的源码、现行文档、ASAR 版本、EXE 与规则资源匹配，以及已撤回模块／文档和个人记录排除，逐项结果保存为本地 `.local/evidence/bo1-rollback-package-check.json`。EXE 品牌检查通过。
- 首次直接打包已验收目录时，发布检查发现测试运行产生的 Python 字节码缓存并拒绝该包；已从按过滤规则收集的源码重新构建。拒绝清单保留为 `.local/evidence/bo1-rollback-prepackaged-cache-rejection.json`。原有先后攻选择图标仍属基线资源，正常保留。

本轮仅清理新建的两个回退验收目录中的可重建资源副本，依据路径、进程、服务锁、清单及哈希逐项确认；清单和结果在 `.local/evidence/bo1-rollback-cleanup-*.json`。既有研究资料、用户记录、失败证据、截图、配置、原始日志与旧包均保留。实机全平台重联和全部原生专项未执行，不能从合成场景推断通过。
