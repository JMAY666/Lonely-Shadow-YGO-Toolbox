# 1.32.0 验证记录

## 范围

同一内置后攻记录的两阶段衔接、安全人工备注、真实资源同步、原生窗口识别及按当时信息复盘。战斗与外部完整状态未接入。

## 检查结果

- `npm run test:modular -- --suite=second-flow` 与 `npm run test:modular:packaged -- --suite=second-flow` 的原生和界面检查均通过：对手阿鲁伯窗口识别、核对用途／公开条件、提示及计划不扣牌、实际交灰、正常抽牌后当前五张、原始起手和人工事件保留、我方续算、真实未响应及连续两张泡影作用于不同对象后的重算。
- 复盘验证：当时手牌、未知信息及计划保持冻结；实际手牌变化、效果无效、目标位置分别记录；对手私有选择不被解码；后续公开信息不改写旧复盘。900px 窗口无横向溢出。
- `npm test`：最终 427 项 Python、217 项 JavaScript 全部通过。另核对计数来源标记为原生菜单，相关 10 项定向测试通过。
- `npm run test:modular -- --suite=second-routes`：上一版内置后攻续算场景回归通过，包括实际改顺序、对手交灰、已用通召和真实抽牌。
- `npm run test:desktop -- --second-only` 与 `npm run test:packaged -- --second-only`：原未关联人工记录、五张手牌案例、历史恢复、先攻入口、P1.2 三类条件比较及实际结果入口通过。本轮未重复全部既有桌面和先攻原生专项；相关基础逻辑纳入全量测试。
- 首轮原生测试完成无响应路径后，因测试从决斗页切换下一场时未恢复训练场地而失败。补齐测试场地恢复后重跑通过，早期失败证据保留；没有把该次失败记为通过。

## 发布和数据保留

使用已核实的离线资源构建 1.32.0 目录包和 ZIP；最终核对程序、版本、规则资源及个人记录排除，证据为 `.local/evidence/bo1-p14-package-check.json`。原生引擎及卡牌脚本未改动。

提交前按精确目录、进程、服务锁和不可变包资源清单核对测试副本。只清理与包中内容逐项一致且可恢复的资源；原始 JSON／JSONL、失败证据、截图、设置、备份、历史包和未知文件保留。清理详情为 `.local/evidence/bo1-p14-cleanup-result.json`。这些本地资料不纳入 Git。

## 保留边界

未认证外部 MD 及其他平台的完整状态、卡池／禁限表；没有战斗、伤害或斩杀结论。视频中未核实名称仍不进入规则注册。所有测试使用隐藏 Electron、隔离数据和内部接口，没有系统鼠标或全局按键输入。

安装包、截图、原生测试记录及失败证据保留本地；源码、文档和脱敏合成测试单独审核。详细操作与信息边界见[两阶段说明](bo1-second-flow.md)。
