# P3 修复后完整标定与预算复核

2026-09-21，继续[可行性计划](ai-learning-feasibility-plan.md)的修复后复验执行包。**修复后二进制完成同一批 20 个训练家族、40 次 B1/T0 运行，全部通过完整前缀重放、原生日志和无损归档审计；T0 仍为 15/20，B1 仍为 13/20。新的保守时间为 4.46 小时/200 家族，超过 4 小时门槛，完整验证也超出剩余 P2/P3 预算，因此本轮没有启动新的验证或 P4。**

## 固定范围与执行

P2 指纹仍为 `a56f903f7331040cd3437dd6dcce63054f178f0effed588ce7188bd908eaf02a`，200 家族的 120/40/40 划分、20 个训练标定家族、目标、B1 和随机条件不变。教师继续为 `T0-resource-v7`，24 节点、12 层、2 秒、束宽 2，原三个候选之外比较合法停止动作，仍扣同一节点预算。没有按旧版部分验证调教师或删除失败家族。

修复后的 `YGOPro.exe` 为 `8d08f492f243bd32bb1831c94a5eed19085fe7f373844b8dbf12629ac1fd30d8`，规则资源集合为 `b6df68764b6c65c7933f291e68d5ed7dceb4cd696a98a69aa05cea671650834b`，共 13,542 文件。参考版二进制为 `40e604e769ce6d9270d7191539757899d6a19bbd66ef1c637821cbffc0d9dc75`。既有学习环境继续使用，没有安装新框架、下载模型或付费调用。

本次从 `82b37d4` 的干净工作区启动标定，命令为：

```powershell
node experiments/ygo_learning/desktop_p1.cjs --suite=teacher --first-family=1 --count=20 --fast
.local/ygo-agent-pilot/.venv/Scripts/python.exe -X utf8 experiments/ygo_learning/report_p3.py .local/ygo-learning/p2-p3/teacher-20260921-130306
```

这一次为一个 20 家族训练批次，实际 worker 594.44 秒，持久进度计费为 595.38 秒，未超过每批 30 分钟。实验只使用自己的隐藏窗口和公开实验条件，完整起手/牌序从旧会话导入；普通应用的随机起手不受影响。新生成的 JSON/gzip、原生 ZIP、会话和源码快照均保存在 `.local/`。

| 项目 | B1 | T0 |
| --- | ---: | ---: |
| 预先纳入家族 | 20 | 20 |
| 达到结构目标 | 13 | 15 |
| 完成首回合 | 19 | 20 |
| 不支持 | 1 | 0 |
| 实际提交窗口 | 593 | 284 |
| 完整重放与原生归档恢复通过 | 20 | 20 |
| 每家族平均耗时 | 10.09 秒 | 13.19 秒 |
| 每家族 p95 耗时 | 15.31 秒 | 18.79 秒 |

B1 的一个不支持家族继续计入分母。T0 20 个回合结束不等于全部达标。此处是重复训练标定，不能把 15/20 对 13/20 当作独立教师收益。

## 完整轨迹复核

新增 `revalidation_p3.py` 离线入口，先独立恢复并审计两版所有训练归档，再核对同一家族的起手条件、策略与资源身份、选择前动态状态、真实回答、后继状态、首次进入第 2 回合的完整原生状态及最终稳定窗口。代码不会运行新的家族、打开留出或创建验证登记。

首次直接比较所有后继快照时，有 11 次运行的最后一条 `after` 不同：旧/新控制器恰好分别读到了第 2 或第 3 回合的窗口，部分已发生下一次抽牌；差异均在首回合最后一次已确认回答之后。该原始比较报告保留。评价始终沿用 P2 的首次进入第 2 回合边界，审计在这个原生日志边界比较完整状态，并单独报告原始后继快照是否相同。真实响应、此前任何前后快照、最终状态及次数仍严格核对；不支持或缺少终场日志时不允许替换边界。

最终 B1 的 593 个实际回答与 T0 的 284 个实际回答全部一致；两者均为 20/20 家族的完整前缀、最终稳定窗口和结果一致。39 次完成首回合的运行，其完整原生终点一致；另一次 B1 在两版中都为不支持、没有首回合终点，不计为已结束。原始后继快照逐项相同的家族数分别为 16/20、13/20，其余 11 次边界后的差异保留在报告中。这种核对只覆盖 20 个已登记训练家族，不证明任意场景下两个引擎等价。

## 两项时间门槛均未通过

| 指标 | 实测或保守预测 | 门槛/判断 |
| --- | ---: | --- |
| 累计 worker | 5,825.97 秒，97.10 分钟 | 所有失败、开发和恢复继续累计 |
| 剩余 worker | 1,374.03 秒，22.90 分钟 | 原上限 2 小时保持有效 |
| 完整 40 家族验证预测 | 3,210.74 秒，53.51 分钟 | 比剩余多 30.61 分钟 |
| 覆盖本次验证所需最低累计额度 | 9,036.72 秒，150.61 分钟 | 未含新的失败诊断预留 |
| 200 家族平均成本外推 | 1.65 小时 | 不能代替保守值 |
| 200 家族 p95、两次尝试规划 | 4.46 小时 | 超过原 4 小时门槛 |
| 达到原 4 小时成本需降低 | 约 10.30% | 仅通过此项仍不满足当前剩余预算 |
| 200 家族保守新增空间，含恢复预留 | 约 4.35 GiB | 当前剩余额度约 7.21 GiB，通过 |
| 标定进程合计采样峰值工作集 | 约 1.33 GiB | 低于 8 GiB；不是连续峰值测量 |

计时包含运行器按原口径计入的归档/恢复等开销。累计 worker 按每批摘要与持久进度的较大值计费；外层 Electron 启动、离线审计、桌面检查和人工分析另计，不能把 97.10 分钟称为所有工作的墙钟耗时。此次比旧版 3.76 小时预测更高，未进行系统调度、批次组织等因素的因果对照，不能断言是焦点修复导致性能下降。

## 下一执行包与待确认方案

旧登记 `validation-registration.json`、旧版 24/40 配对、失败与恢复记录原样保留。此次新增完整训练标定，不补齐旧版剩余 16 个验证家族，也不把修复样本混入独立结果。`next_sampling_authorized_by_gate` 仍为 `false`。

若保持原上限，下一步须继续降低真实执行成本，且所有新 worker 仍扣剩余 22.90 分钟。已经准备的另一具体方案是：**将 P2/P3 累计上限调整为 3 小时，将 200 家族累计成本上限调整为 5 小时**，仍然零付费，保留每批 30 分钟、内存 8 GiB、目录 20 GiB 和全部质量门槛。新增预算足以覆盖当前完整验证保守预测并留出部分失败处理空间；这只是待确认方案，本轮没有修改或生效这些上限。

预算获得明确确认后，才建立绑定修复后版本的新登记，保留原登记，以全部 40 个验证家族、八批各五个配对完成选型；任何可靠性失败立即停止。验证仍须净增加至少两个达标家族、共同支持范围不退化，以及所有重放/归档通过。完整验证和成本均通过后才考虑 P4。holdout、学生训练、LLM 对照、正式 2,000 家族、能力更新和产品建议入口仍未执行。

## 本地证据与检查

- `.local/ygo-learning/p2-p3/teacher-20260921-130306/`：修复后 40 次执行、完整随机条件、冻结源码及归档。
- `report-20260921-131525/summary.json`：20 家族完整审计和新成本。
- `revalidation-20260921-132506/summary.json`：保留首次严格逐快照比较的 11 处终场后观察时序差异，不作恢复通过依据。
- `revalidation-20260921-134343/summary.json`：最终完整比较、二进制绑定、原登记保留与累计预算；旁边保存本次审计器源码，SHA-256 为 `91cdfbf09e6320fe8b0ed4f21ca909e4535f1edd2f1f7079d02fe710d7ef81f4`。
- `checks-revalidation-20260921/`：学习测试、最终离线审计、桌面回归和本次交付核对记录。

已通过 75 项学习实验测试（最终边界保护另复跑相关 9 项）、应用 446 项 Python 和 220 项 JavaScript 测试，以及 `npm run test:desktop -- --native-only`、`npm run test:packaged -- --native-only`。两版桌面均通过焦点锁竞争、真实引擎/持久化和正常启动拒绝内部测试接口。此次只新增离线实验审计与文档，原生源码未改，复用了已核实的原生构建和 1.41.0 目录包，没有新打发布 ZIP。未执行 GPU 训练、完整模块化专项或独立 CPU 安装环境验收。104 个本地文档链接检查通过。

排除规则沿用 `.gitignore`。提交前确认项目运行进程已退出并核对绝对路径与测试标记，删除 165 个可重建字节码、140 张已关闭测试的过期原生截图，合计 305 个文件、110,233,568 字节（约 105.13 MiB）；本次检查的合格会话保留最新两张截图，共 28 张。完整 JSON/JSONL、个人数据、旧版证据、恢复点、原始归档、模型和环境保留。代码、脱敏报告和文档可进入 Git，原始实验与桌面正式验收文件继续仅保存在本地。
