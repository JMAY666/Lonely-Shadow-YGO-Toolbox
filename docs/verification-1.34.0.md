# 1.34.0 验证记录

## 实际客户端

2026-09-19，用户自行操作已适配的 YGOPro 1.036.2 x64；镜像指纹与既有配置完全一致。工具只读进程，不控制客户端或发送对战操作。

- 用户逐项确认：我方 6 张手牌及重复份数、双方 LP、对手三只公开怪兽和一张未知盖卡。
- 用户报告发动魔法并被无效；随后读取到手牌 5 张、相应魔法在墓地和 LP 变化。无效类型未由读取器解码，不将玩家报告冒充自动裁定。
- 用户报告通常召唤；随后读取到手牌 4 张、对应我方表侧怪兽和稳定 LP。
- 读取过程中遇到变化时拒绝混合快照。早期记录还包含对局结束、准备和重新开局阶段；不据此宣称完整跨局产品流程均已实测。
- 发现并修复默认模块名错误；位置检查初版未支持组合表示 `5/10`，曾拒绝有效快照，补齐后实机读取成功。

原始读取、实际卡号、卡组及用户核对记录位于 `.local/ygopro-p21-live/`，保留本地，不进入 Git 或合成夹具。本次真人验证针对读取器，完整桌面“自动开局 → 预览 → 采用”的端到端真人流程仍待后续。

## 隔离检查

- 最终 `npm test`：447 项 Python、219 项 JavaScript 全部通过。
- 二进制边界检查覆盖私有卡号不可读、位置组合、字段变化、容器范围、进程重启、两份快照不同和默认模块定位。
- 状态检查覆盖预览不改资源、确认保留起手与历史、重复请求只应用一次、保存失败不覆盖、场景／局次变化、轮询发现偏差及拒绝重复手填资源。
- `npm run test:desktop -- --second-only` 和 `npm run test:packaged -- --second-only` 均通过原人工／条件提示流程和新的合成公开资源界面；界面测试不冒充真实外部客户端验收。
- 收起外部资源记录中不可用的内置路线区域后，两种版本的 `--second-live-only` 定向界面回归通过；回合玩家保持待核对、900px 布局无横向溢出。已检查两种窗口宽度截图。

最初状态测试夹具缺少后攻确认字段，被开局校验正确拒绝；补齐夹具后定向检查通过。重复运行界面夹具时，同名测试构筑被拒绝覆盖，改用每次独立名称后重跑通过。失败输出保留，没有记为通过。

## 范围与发布

原生引擎、卡牌脚本与规则案例未修改。阶段、回合玩家、完整事件／连锁、素材、实例次数与外部规则认证均未完成，P2.1 整体不标为完成。MD 和其他客户端的公开资源适配未开放。

使用后台 Electron、隔离测试数据及应用内部接口。安装包和验收证据保留本地；提交前只清理核对过的可重建测试资源，保留真人数据、JSON／JSONL、恢复材料、截图、设置、备份和旧版本。

最终目录包与 ZIP 逐文件核对程序、文档、ASAR 版本、EXE、不可变规则资源及个人记录排除，记录为 `.local/evidence/bo1-p21-package-check.json`。清理仅涉及本轮开发／打包两个隔离测试目录中内容哈希一致的包资源副本，进程、服务锁和路径核对见 `.local/evidence/bo1-p21-cleanup-result.json`。`.gitignore` 已覆盖真人资料与安装包，无需扩大排除。

本轮未重复全部既有原生推演和其他平台真人专项；相关基础逻辑纳入全量单元检查，内置规则核心和脚本未变更。
