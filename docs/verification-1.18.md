# 1.18 验证记录

日期：2026-09-16。测试使用隐藏的 Electron 窗口、原生内部接口及隔离数据目录；未操作用户正在使用的窗口。

| 检查 | 结果与范围 |
| --- | --- |
| `npm test` | 212 项 Python、157 项 JavaScript 测试通过；覆盖终场标记映射、排序、收藏持久化与乱序响应、缓存隔离与失效、步骤边界、图示和原有逻辑。 |
| `npm run test:desktop` | 完整桌面回归通过；卡组、教程、原生场地、回退、素材、方案保存、导入导出、妥协分支及正常重启。 |
| `npm run test:packaged` | Windows 发布包完整回归通过；内置 Python、程序品牌及正常启动时内部测试接口关闭。 |
| `npm run test:modular -- --suite=core` | 原有模块化搜索、偏差后续及来源更新通过；新增真实转生炎兽 Combo 3 Step 9 后续计算，保留前缀且不重复通常召唤。 |
| `npm run test:modular -- --suite=pipeline` | 折叠恢复、缓存重排、普通步骤续算、收藏与标记终场、无实战输入、结束释放通过。发布包对应专项同样通过。 |
| `npm run test:modular -- --suite=forecast` | 真实抽牌、随机堆墓、重复卡牌填报、灰流丽处理及确认进度保留通过。发布包对应专项同样通过。 |

图示另在隐藏渲染器中检查了素材箭头、场地区域小图、场地魔法发动标签、Step 9 前缀及标记终场；单独的场地魔法放置不重复画成两张卡。最后的收藏乱序响应和标签调整另有针对性测试。

发布包专项的一次合成组合测量中，搜索耗时从 3.060 秒降至命中候选缓存后的 0.003 秒，后者没有再次调用引擎；改变来源集合后重新搜索，复用 70 次步骤校验。这是该测试组合的搜索耗时，不是任意卡组的首次生成速度或按钮到页面显示的总时间。

首次开发验收遇到测试页面未切换到展开模块、Step 9 测试请求未编码卡组路径空格，以及一次没有输出的测试子进程退出；前两处验收脚本已修正，后者重试后通过。上表记录最终完成的检查，未把中断的尝试算作通过。

缓存只存在本次应用进程内，按会话、规则版本、完整路径和实际填报区分，结束本局释放；新路径仍需规则核心验证。来源不完整、前缀含未填报随机结果或本局无法重放时保留原教程。未标记卡牌不列为有效终场，标记效果数量不等于已验证阻抗次数。

截图、测试记录、合成方案和 Windows ZIP 均保留在本地 `.local/`、`release/`，不纳入 Git。原用户数据和旧版包保留。用户当前使用中的窗口及个人实际对局不属于此次自动化验收。
