# 1.28.5 验证记录

## 变更

- 将历史未保存草稿数量从“展开方案调整与保存”导航移到“展开管理 → 历史记录与未保存草稿”，明确显示“未保存草稿 N”，为零时隐藏。
- 仅表示张数的随机展示、卡组顶部翻牌和抽牌合并为一张卡背及数量。独立效果、对象、素材、不同去向、条件牌和有单张说明／终场标记的卡牌保持分开。
- 回看简略／详细日志、手动／自动教程、SVG／PNG 一图流共用数量显示规则。卡背详情保留逐张记录及返回上一级，原始报告不改写。
- 步骤悬停改为关键卡牌缩略图及操作摘要，最多两项；空间小时继续精简，无法容纳时不显示截断窗口。点击步骤查看完整效果与备注，保留原有步骤点击、卡牌详情、快捷键、分支和分隔栏范围。

## 已执行检查

- `node --test tests/*.test.cjs`：207 项 JavaScript 测试通过，覆盖数量合并边界、抽牌、后续不同去向、实例说明和冻结记录保留。
- `npm run test:desktop -- --tutorial-presentation-only`、`npm run test:packaged -- --tutorial-presentation-only`：通过。覆盖空白调整页、草稿计数从 0 变为 8 再归零、单卡背 ×5、逐张详情、原有指定卡牌展示、导出 SVG／PNG 及报告不变。
- `npm run test:desktop -- --duel-only`：通过。覆盖手动教程、数量展示、悬停摘要、嵌套卡牌详情、长备注下的慢速点击、前进／后退、分支、快捷键及窗口缩放。步骤预览没有内部滚动和内容截断。
- `npm run test:desktop -- --automatic-workspace-only`、`npm run test:packaged -- --automatic-workspace-only`：通过。覆盖自动教程的数量展示、悬停摘要、步骤与分支导航、分隔栏、快捷键、不同窗口宽高、独立进度及源记录保留。
- `node node_modules/electron-builder/cli.js --win --x64 --dir`：生成 1.28.5 程序包。沿用经 SHA-256 核对的 1.28.4 原生资源包与内置 Python，本次未重新编译引擎。EXE 品牌检查通过。
- 8 个修改过的程序包网页文件与源码 SHA-256 一致；README、操作手册与 Git 忽略规则检查通过。

后台验收仅操作应用创建的隐藏窗口与隔离数据，截图来自应用渲染器。未使用系统鼠标、全局按键或用户实际牌组、对局记录。本次未修改 Python 服务或原生规则实现，未重新运行全量 Python／原生规则测试。

程序包和验收证据保留在本地忽略目录，不进入 Git；已有用户数据、备份及旧版本保留。

本轮已清点 7 个隔离测试目录中的可重建资源。自动执行策略拒绝了删除命令，返回 `blocked by policy`，因此没有执行清理；资源、记录及证据均留在本地忽略目录。
