# 1.30.0 验证记录

## 范围

P1.2 条件化交康提示：结构化来源／效果／窗口、次数与保留用途、交与不交比较、独立提示与计划日志、具体发动及关联处理结果、资源／窗口过期保护。

## 检查结果

- `npm test`：408 项 Python、214 项 JavaScript 测试通过，包含原先攻流程、后攻观察及新增提示测试。
- `npm run test:desktop -- --second-only`、`npm run test:packaged -- --second-only`：均通过。覆盖 P1.1 的五张起手→使用一张→抽一张后仍五张且原始起手不变；新增阿鲁伯窗口中泡影／灰流丽取舍、计划不扣资源、实际发动及关联结果、实际变化使旧提示失效、恢复历史及窄窗口。截图来自应用渲染器。
- `npm run test:modular -- --suite=second-hints`、`npm run test:modular:packaged -- --suite=second-hints`：开发和打包版各 14 个真实原生案例通过。阿鲁伯／莫邪／烙印融合各三种手坑和不响应，另加朱光与绿光的发动无效。烙印融合发动无效后第二张能出现于合法菜单，效果无效后不能；幽鬼兔破坏后的检索／衍生物处理继续，莫邪案例不能立即用原莫邪同调召唤赤霄。
- 规则摘要核对完整数据库、完整 Lua 脚本集合、核心及相关卡文；数据库指纹与原生会话冻结来源一致。打包版的全部案例结果及资源指纹与随程序提供的最小摘要逐项一致。
- 审核追加验证：原生案例拒绝的响应不能被人工勾选强行放行；未评估收益不记为零；对手有其他怪兽时不推断幽鬼兔能阻断全部同调；较早／跨回合结果不能返还较新次数；规则资料在生成或采用时变化会拒绝旧结果。最终全部单元测试覆盖这些修改。

一次早期全量运行在 `test_resume_refuses_conflicting_target` 临时目录内重命名 `migration.json` 时出现 `PermissionError [WinError 5]`，原因未确定。随后单独运行全部 9 项桌面数据测试及最终全量测试均通过；失败和重跑日志均保留，没有隐去该次失败。

## 发布与本地数据

发布使用已核实的离线原生资源包。目录包用于上述打包版验收；最终 ZIP 重新打包最新源码，并核对源码／目录包／ZIP 中程序文件一致、版本及个人记录排除情况。构建和核对详情保存在本地证据，不将安装包纳入 Git。

提交前核对绝对路径、进程和资源清单，清理四个已结束的隔离验收目录中可由原包恢复的运行资源，共 115,548 个文件、5,115,735,684 字节（约 4.76 GiB）。观察记录、JSON／JSONL、正式截图与证据、设置、备份、资源清单和所有版本包保留；排除规则继续适用，无个人数据迁移。

本地证据：`.local/evidence/bo1-second-p12-final.log`、开发／打包版 `second-result.json` 与 `second-hints-native.json`、`bo1-p12-package-check.json`、`bo1-p12-cleanup-result.json`。

## 边界

外部平台、MD 卡池／禁限表、多重连锁、额外保护／持续限制和完整局面重建仍未验收，提示会说明范围。后攻续算与斩杀属于 P1.3/P1.4，尚未实现。原生固定手牌只用于机制验证，不代表 MD 合法构筑；本地案例通过不等于当前实战合法或策略最优。本轮界面专项覆盖后攻及其先攻入口兼容，没有重复运行全部既有桌面专项。

测试只使用应用创建的隐藏窗口、隔离资料和内部接口，没有全局鼠标／按键输入；原始证据和包保留本地，必要的最小案例元数据随源码保存。
