# 1.49.6 通常陷阱第四组验收

日期：2026-09-26。从20张私有候选断点继续，固定100张均已逐卡处理，接纳95张；累计2,488／14,716，尚余12,228。本阶段不是全库完成声明。

[完整清单](card-annotation-batch-2026-09-26-traps4.json)保留卡号、当前卡文身份与来源、实际查询和规则边界。盗墓者（61705417）、黏着的落穴（62325062）、小子防御（10759529）缺少具体边界依据；王家的财宝（63571750）、伤害瘦身（95448692）在同一无编号段有两个独立发动位置和处理，需要不虚构获赋关系的子效果模型。这5张保留私有完整候选，不升reviewed。旧2,393条逐项不变。

## 规则与整合

- 迫使玩家送墓作用于玩家，不标直接送墓动作或TAG；仍保留有依据的解场候选。只在己方攻击宣言响应窗口适用的封锁不视为持续终场干扰。
- 投币表／里效果对调只作用于已经实际投币获赋的对象，不重新投币。伤害转回复只改变所规定伤害事件，不借用直接回复或效果无效。两只指定怪兽战斗计算不标效果烧血。
- 对方除外状态、场上返回持有者手卡作为费用与效果回手分开；动态LP规则保留处理时计算依据，不执行表达式、不标回复或伤害。
- 义贼的极意书后续随机弃手另开陷阱效果连锁，不在父层发动时立即处理，也不把通常怪兽变为效果怪兽。固定子处理保留原陷阱来源，费用和时点不跨层拼接。
- 自由解放一个对象离场仍处理另一对象；毒蛇供物任一对象离场则不处理。电磁轨道炮完整官方卡文明确原本ATK，简写补足没有构成相反规则，已按原本ATK确定动态对象上限。

## 检查

- 实际批次门禁：95张、147条查询、23条规则断言、3条用途断言、436个来源文件SHA-256通过。
- 全库卡文／完整分段／词表／引用校验：2,488条通过；20系列／35样例通过。
- 独立于生成器的机械重验：95张身份与官方卡页／FAQ正文、99项备注引用、14项通用TAG检查通过；旧2,393条逐项保留，不声称全部特殊裁定进行了第二次穷尽审查。
- 完整`npm test`：最终代码与资料 PASS 656项Python、256项Node；受控动作参数负例、动态LP互斥协议、对方除外区、回手费用及用途候选回归通过。
- 后台开发版：PASS `npm run test:desktop -- --annotations-only`；玩家要求／直接动作、原陷阱后续效果时点、动态LP文字、待核实条目、个人修正与布局通过，errors为空、globalInput=false。
- 当前1.49.6构建：PASS `npm run build:dir`；asar版本1.49.6及8个应用源码／数据逐字节一致。
- 当前打包版桌面验收：PASS `npm run test:packaged -- --annotations-only`；EXE名称／描述／7帧图标通过，应用在隔离数据及无开发者PATH条件下完成与开发版相同的新增规则、范围、后续效果和个人修正检查，errors为空、globalInput=false。

原始采集包、辅助身份索引、私有生成器、断点及证据保留本地，不读取或改写真实用户的个人修正、牌组、方案和备份。未恢复封存的AI学习计划。

补充审查明确了控制者与原持有者的区别：玩家选其控制怪兽后，去向依卡的原持有者决定，不能固定成该玩家墓地。对应官方FAQ11875原始HTML、提取文本及元数据保存在`.local/annotation-integration-20260926/traps04-general/`，规则字段修正后已重跑检查并重新构建。

最终日志在`.local/annotation-integration-20260926/traps04-*`，开发证据为`.local/evidence/electron-development-annotations-annotations-1496-development-98c9f148e96e43e480ea9617d3fc2a4c/`，打包证据为`.local/evidence/electron-packaged-annotations-annotations-1496-packaged-473dc27a5901423daef68ddc52d58b84/`。文档最终结果同步至新包notices，应用代码／数据字节不变。
