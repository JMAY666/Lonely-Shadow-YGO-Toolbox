# 1.47.1 验证记录 · 紧凑系列夹、发售排序与卡图浮层

日期：2026-09-24。范围：92px 紧凑系列目录、每页 48 夹、按最早已知实体发售时间排序、左侧卡图缩略图、右上角按点击位置打开的原图浮层。

## 检查结果

- `npm test`：568 项 Python、245 项 Node 测试通过；记录在 `.local/series-1471-tests.log`。日期专项覆盖最早区域首发、临时／异画卡号映射、未知与未来日期、筛选不能改变系列首发、空响应拒绝覆盖、冲突数据与歧义映射处理。
- 最终浮层修正后，16 项 `card_annotations_view.test.cjs` 前端专项再次通过。覆盖日期升降序、同日中文排序、未知日期置后、列表缩略图、默认关闭浮层与窗口边缘定位。
- `python scripts/check_annotation_series.py --runtime .local/YGOPro-Lite` 和 `python scripts/check_card_annotations.py --runtime .local/YGOPro-Lite` 均通过，五个系列七张展示样例及原有 46 张效果标注保留。
- `npm run test:desktop -- --annotations-only`：隐藏 Electron 专项通过。新增检查 92px 文件夹、48 项分页、三种排序、缩略图加载、右上角点击位置浮层、正文高度不变、再次点击关闭、外部点击关闭、键盘 Enter／Esc、关闭按钮及 760px 窗口内完整显示；原有筛选、别名、个人备注与草稿回归仍通过。
- 首次专项发现原生浮层在再次点击触发按钮时被 light-dismiss 关闭后又打开；补上原生 `popovertarget` 关联并阻止重复默认动作后复测通过。浮层背景改用项目已定义的主题变量，打包版另验证其背景不透明。
- `npm run build:dir` 构建 `release/1.47.1/win-unpacked/` 成功。`npm run test:packaged -- --annotations-only` 通过，EXE 品牌／图标检查通过；最终五个实现与日期资料资源逐字节匹配工作区。最终 CSS 背景修正同步到包内后完成打包专项，后续完整构建可从源码复现。
- 开发版与打包版均 `errors=[]`、`globalInput=false`。已检查紧凑文件夹、左侧缩略图和原图浮层截图；证据保留在 `.local/evidence/electron-{development,packaged}-annotations-series-1471/`。没有操作用户的 1.47.0 窗口。
- 未重跑决斗与模块化求解器专项，本轮未修改其行为；封存的 AI 学习计划未启动。

## 发售资料与覆盖

日期来源与抽查链接见[制作流程](card-series-production-guide.md)。精简资料包含 14,487 张公共卡片的 OCG／TCG 首发日期与 4,086 个明确卡号映射。全表与缓存的公开响应重新提取结果一致，原始响应 SHA256 为 `00725482bd9eb743648f510a159ca85a43806505c7c62783bf95af07648c5d61`。原始响应、卡文、图像地址与价格均不进入新资料文件；应用正常使用完全离线。

基础卡库 595 个系列均至少匹配到一个成员的日期。只读核查当时本机已安装公共补丁卡库：603 个系列中 598 个有已知日期；盈彩月夜、异解△、龙札、白鸦、轮回虫龙五个系列未能匹配，显示「日期待补」并置后。没有用系列编号推测日期。某系列匹配到日期不等于每张成员的日期都完整，提示中保留覆盖数量；最早成员日期也不保证等于系列名称被正式定义之日。

## 文件保留

README、体系规范与制作流程同步更新。`.gitignore` 已核对，现有 `.local/`、`release/`、缓存与个人资料排除规则继续适用。原卡库、卡图、个人 TAG、备注、备份和旧版本保留；公开日期资料按用途纳入 Git，约 0.78 MB。新版本另存 1.47.1，不覆盖运行中的 1.47.0。

两个本轮验收实例退出后，逐个检查绝对路径、重解析点、运行进程和资源清单 SHA256，删除可重建的测试资源 57,774 个文件、2,557,894,466 字节（约 2.56 GB）。个人测试备注、设置、备份、JSON/JSONL、正式截图、原始资料快照和正式程序包保留。记录在 `.local/evidence/series-1471-cleanup.json`。GitHub About 已核对，现有描述仍准确。
