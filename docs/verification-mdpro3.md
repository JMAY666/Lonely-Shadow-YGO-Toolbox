# MDPRO3 适配验证（2026-09-18）

本次新增 MDPRO3 智能化识别，并按用户要求统一缩小三个平台入口。构建指纹、功能边界和三轮真实核对见 [适配说明](mdpro3-recognition.md)。

为隔离共享工作区中同时进行的其他功能修改，先从 `907f07b` 建立独立检出，只验证 MDPRO3 相关改动。随后其他功能在 `adc2557` 完成提交并推送，又对合并后的主线 1.28.1 运行单元测试、开发版和打包版三平台专项，并生成 `release/mdpro3-1.28.1/` 下的最终安装包。独立检出的早期验收包保留 1.28.0 版本号；两个目录及旧安装包都留在本地，没有覆盖其他任务的发行目录。

| 检查 | 结果 |
| --- | --- |
| `npm test` | 独立检出 348 项 Python／198 项 Node 通过；合并主线 359 项 Python／200 项 Node 全部通过 |
| `npm run test:desktop -- --smart-only` | 独立检出与合并主线通过：三个平台的识别交互、跨局、取消、迟到结果、冻结起手、方案匹配与计算调用 |
| `npm run test:packaged -- --smart-only` | 独立检出与合并主线的打包版均通过相同三平台专项；开发工具不在打包进程 PATH 内 |
| `npm run test:packaged -- --automatic-only` | 独立检出打包版通过：原逐步卡组识别、保存／覆盖、TAG、先后攻和起手流程 |
| 平台视觉专项 | 开发版与打包版在 900／1280／1600 宽度下检查 SVG 离线解码、各平台背景、等宽方形及桌面并排；最终截图已查看 |
| 应用构建和资源核对 | 两个 ZIP 构建通过；独立检出 106 个、合并主线 107 个打包应用文件与对应源码逐一一致 |
| 打包 Python 真实读取 | 成功连接当前 MDPro3 并读取正式先攻状态；只核对当前状态，不计为第四轮初始发牌验收 |
| 三套不同构筑真人联调 | 40/15/15、44/15/0、48/15/3 均自动校验通过；同一订阅连续识别，用户逐轮确认 5 张起手完全一致 |
| 隐私与 Git 范围 | 只提交源码、合成测试、必要图标和说明；个人构筑、手牌、截图、进程信息、工具、元数据研究及验收包留在本地忽略目录 |

最初的共享工作区桌面专项因旧测试只匹配不带查询参数的资料库地址而超时；测试改为兼容带查询参数的请求后，最终开发版和打包版均通过。共享工作区完整 `npm run test:desktop` 曾启动，因同时存在持续变动的其他功能而中止，不计作本次完整基线通过；上表是独立检出及合并主线实际完成的验证范围。

后攻、真实进程重启、BO3、双打、观战和录像没有进行本次 MDPro3 真人联调。异常模式拒绝、进程身份变化和后攻起手由合成测试覆盖。真实联调资料库没有匹配的模块化展开来源，页面准确显示缺失原因；识别通过不等于已生成展开路线。
