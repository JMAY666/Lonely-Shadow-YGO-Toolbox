# 1.47.0 验证记录 · 系列卡牌夹与彩色标注

日期：2026-09-24。范围：系列目录浏览、中文名与别名、五套徽记及七张代表卡、彩色效果 TAG、额外怪兽类型符号、默认收起筛选与原卡图，以及后续智能体制作流程。效果结构和既有 46 张效果标注保持原状。

## 已执行检查

- `python scripts/check_annotation_series.py --runtime .local/YGOPro-Lite`：五个系列、七张代表卡的来源引用、徽记、封面、当前卡库系列编号与类型一致。
- `python scripts/check_card_annotations.py --runtime .local/YGOPro-Lite`：原有 46 张效果标注的词表、来源、卡文快照与分段覆盖通过。
- `npm test`：562 项 Python、242 项 Node 测试通过，日志保留在 `.local/series-1470-tests.log`。新增系列专项验证父／子系列、兄弟子系列隔离、多系列、带符号 64 位编号、未知系列、支援卡文不充当归属、中文别名、样例状态、计数与封面一致及资源重载；前端覆盖复合类型、彩色标签转义、中文排序、筛选和原图默认收起。
- 最终接口参数与导航高亮复核后，26 项标注／系列 Python 专项和 13 项前端专项重新通过。最初一个前端断言把「灵摆不等于额外卡组」提示中的字词误判成标识，已改为检查实际额外标识元素。
- `npm run test:desktop -- --annotations-only`：后台 Electron 专项通过。覆盖默认系列首页、中文排序与别名检索、目录分页、无系列归属、封面加载、系列与类型徽记、延迟加载原卡图、彩色 TAG、筛选结果及已选计数、个人备注和核对状态保存、其他分段未保存输入保留、草稿撤销、无效果与未标注卡、空结果清除、键盘选卡、刷新保留条件、760px 窄窗口。
- `npm run build:dir`：1.47.0 目录包构建通过，位于 `release/1.47.0/win-unpacked/`。五个变更的 trainer 资源文件与工作区逐字节一致，新指南与验证记录随包附带。
- `npm run test:packaged -- --annotations-only`：后台打包版同项验收通过，EXE 品牌与图标检查通过。开发版和打包版均 `errors=[]`、`globalInput=false`。最终修正「进入系列夹时高亮系列浏览入口」后，两版重新验收。
- 已人工检查默认卡牌夹、超量样例详情及窄屏卡牌夹截图，布局与加载正常。证据在 `.local/evidence/electron-{development,packaged}-annotations-series-1470/` 中；`annotations-result.json` 为专项结果，四张 `annotations-*.png` 为页面截图。
- 本轮未修改决斗／训练／路线求解器，未重跑完整默认桌面流程及模块化求解专项；未启动封存的 AI 学习计划。

## 范围与数据保留

当前基础卡库包含 14,981 张，排除衍生物后可浏览 14,716 张，建立 596 个目录（含「无系列归属」）；多系列卡可出现在多个目录，目录数量不是互斥分类计数。五个系列的官方简体中文名经过联网核对；只有七张代表样例核对了对应归属和类型，其余译名与归属来自卡库。没有批量新增完整效果标注。

`.gitignore` 已检查，现有规则覆盖 `.local/`、`release/`、个人资料和备份，无需修改。只提交源码、资料、测试与文档；卡图复用原本本地资源，原卡库、卡名、效果标注、个人 TAG、备注、旧版本和备份保留。README 与智能体交接指南同步更新。

## 发布前清理

确认本轮两个测试实例均已退出，逐个核验路径在本项目 `.local/` 内、所有路径层级无重解析点，以及文件大小／SHA256 与各实例资源清单一致，仅删除可从程序包恢复的测试资源：57,774 个文件、2,557,894,466 字节（约 2.56 GB）。保留 `system.conf`、个人标注、设置、备份、JSON/JSONL、正式截图和验收结果；原始卡库、正式发行包与旧版本保留。四个已清理的 1.46.0 验收副本也核实未重新留下原生运行程序，没有扩大删除到其他用途不明目录。清理记录位于 `.local/evidence/series-1470-cleanup.json`。

GitHub 仓库简介通过公开 API 核对，现有功能描述仍准确，无需改动。现有 origin 地址由 GitHub 重定向到同一仓库 `JMAY666/Lonely-Shadow-YGO-Toolbox`，目标仍为 `main`；不修改远程配置。
