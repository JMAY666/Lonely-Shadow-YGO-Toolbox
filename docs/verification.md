# 验收记录（2026-09-13 至 2026-09-14）

## 可回退时间轴 1.3.0（2026-09-14）

- `npm test`：93 项 Python、45 项 JavaScript 测试通过。新增路线选择、连续回退与新分支、保存路线隔离、过期／重复请求、恢复回执丢失校对、连锁合并、延迟依据仍保持步骤顺序、当前节点与恢复锁、切换会话丢弃迟到响应、原生左栏裁剪及场地命中检查。
- `npm run test:desktop` 与 `npm run test:packaged`：最终构建各 26 项完整后台流程通过，均无渲染器错误。开发版与打包版结果分别保存在 `.local/evidence/electron-development/result.json` 和 `electron-packaged/result.json`，并核对测试记录中的引擎哈希与最终包一致。只使用各自隔离的 Electron 页面和原生内部事件／截图接口，不使用系统鼠标或全局按键。
- 从已完成的抽牌和通常召唤回到中间节点，再回到初始状态、重新前进到尚未替换的节点，逐项比较完整区域快照。重新召唤到不同主怪兽区后，旧节点退出有效路线，抽牌统计和当前记录没有重复；原始日志中的旧路线仍存在。
- 使用真实“强欲而贪欲之壶”验证 10 张里侧除外费用和抽 2 张。回到发动之后，手中另一张同名卡仍不能发动；回到发动之前重新发动，除外副本、抽到的卡、牌组顺序与实例编号完全一致。
- 使用“增援”完成检索和真实洗牌，并在选卡／连锁处理中回到前一个节点；重新选择可正常结算。“愚蠢的埋葬”送墓、“简易融合”支付 1000 LP 并融合召唤千眼纳祭神之后，回到检索节点恢复此前完整状态，改为先融合的路线不再包含已撤销的送墓步骤。
- 灰流丽与强欲之壶的两段连锁合并到同一个完成节点，仍保留报告中无效后送墓的独立动作。回退恢复双方状态，再次操作的完整结果一致；没有为固定步骤数删除原始依据。
- 在独立候选核心重演、校验完成后注入测试失败，确认回退前的完整场面保持不变，并能继续通常召唤。故障注入文件、输入和截图接口只在显式测试模式生效；普通随机训练仍拒绝测试接口。
- 结束并保存后，最终方案只包含有效路线；其他已保存方案、来源构筑和独立原始日志通过原有保存、修改、删除及重启回归检查。
- 原生补丁应用、VS x64 构建、JavaScript 语法检查及 Windows ZIP 构建通过。对包进行 CRC、原生引擎哈希、trainer 源码一致性、ASAR 版本和个人数据范围检查；结果保存于 `.local/evidence/timeline-package-verification.json`。`.local/`、`release/`、依赖及测试证据继续排除在 Git 外，没有删除用户数据。

验收中修复了 Windows 状态标记的并发读取导致原子替换失败的问题：短暂共享冲突重试，辅助状态写入失败不终止正常展开。测试本身也修正了动画未结束时读取表示选择按钮，以及将“两段连锁”误等同于“报告恰好两条动作”的断言。最终保护还包括保持原消息读取的动态缓冲扩容，以及延迟动作依据不会使步骤 2 显示在步骤 1 之前。

范围：初始节点为首次玩家决策点；其余节点为连锁为空时的主要／战斗操作选择，尚未完成的选择／结算可以退出到此前节点。底层没有完整状态反序列化接口，使用同一存活尝试的调用序列重建独立引擎，校验后切换；不把关闭后的历史 JSON 快照称为可继续操作的引擎状态。未逐卡穷举所有 Lua 脚本或第三方扩展；不一致的重演会拒绝恢复。

## 可配置展开 1.2.0（2026-09-14）

- `npm test`：84 项 Python 和 42 项 JavaScript 测试通过。覆盖起手数量与副本守恒、单张禁止、全指定冲突、缩减数量保留超量选择、移出主卡组、可抽数量不足、LP／计时范围、卡组编辑重复点击与失败恢复、新配置持久化、旧方案默认值、删除失败及目标隔离。
- `npm run test:desktop` 和 `npm run test:packaged`：各 23 项完整后台流程通过。全部操作仅使用隔离 Electron 渲染器和原生引擎内部验收接口；两份结果均为 `globalInput: false`，无渲染器错误。
- 真实引擎分别发出 1／2／3／5 张初始手牌；单张起手的禁止条件生效，双方 LP 实际为 4000／6500。正计时从指定 15 秒开始，倒计时 1 秒归零后停止并提示但原生场地仍可操作，关闭模式隐藏显示。返回后计时停止，重开使用新 UUID 和新配置。
- 对手卡组使用现有选择器和卡组编辑操作，更改只进入本次训练快照；移除已指定卡牌、缩减起手后均提示调整。真实对手支持 2 张起手，其中一个槽位随机补齐；来源 YDK 保持不变。
- 开启可选响应时，灰流丽实际发动并阻止强欲之壶抽卡；关闭时保留灰流丽在对手手牌，强欲之壶正常抽 2 张。关闭后 AI 自己的回合仍通常召唤。
- 玩家选后手、双方各指定 1 张：对手先通常召唤三眼怪，玩家进入第 2 回合时按规则抽 1 张，实际手牌为 2 张。黑洞破坏三眼怪后，其强制送墓检索真实发动并完成；没有因响应开关关闭而跳过。
- 调整页实测保存、修改备注、恢复全部配置、取消删除、模拟删除失败后保留内容、确认删除，以及放弃草稿。其他正式方案、来源卡组和原始操作记录保留；旧方案缺失新配置字段时的恢复通过无迁移测试。
- 对已有两个本地数据目录的 24 条会话元数据和 58 份历史报告做只读 JSON 解析，均可读取，没有改写这些文件。证据为 `.local/evidence/configuration-compatibility.json`。这里不把 JSON 可读性等同于逐条重新操作历史对局。
- 原生补丁应用、VS 2026 x64 编译和 JavaScript 语法检查通过。验收过程中修复了保存后导航误拦截与卡组应用重复点击的状态问题；强制效果测试曾误用卡号，已按本地数据库和 `c26202165.lua` 校正，并完整重跑通过。

打包版使用同一套 23 项流程，在 `.local/desktop-check-packaged/` 运行；开发版和打包版证据分别位于 `.local/evidence/electron-development/` 与 `electron-packaged/`。仍沿用基础 AI 策略，本次未逐一实测所有卡牌和复杂卡组的策略质量。

## 展开方案流程 1.1.0（2026-09-14）

- `npm test`：77 项 Python 测试和 37 项 JavaScript 测试通过。新增覆盖副本计数、不放回抽样、指定表示至少包含、整副起手禁用、冲突及不足提示、冻结牌组重试、草稿与正式方案隔离、保存失败重试、重复保存、保存后刷新失败及删除后迟到请求。
- `npm run test:desktop` 与 `npm run test:packaged`：各 18 个完整流程检查通过，均使用后台 Electron、内置 Python、真实原生引擎及独立的验收数据目录。没有系统鼠标、全局按键或其他应用窗口操作。
- 实际走通 YDK 导入保存 → 前置设计 → 展开 → 重新展开 → 结束生成草稿 → 修改名称与备注 → 保存 → 管理查看和确认删除；模拟一次保存请求失败，文字保留后成功重试，同一草稿重复请求仍只有一个正式方案。
- 真实发牌与前置设计记录的起手逐张一致，主卡组 40 张发牌后剩 35 张。第一次尝试发动强欲之壶后重试，双方初始快照完全一致，新日志不含被放弃的效果步骤。
- 禁用验收牌组为 5 张强欲之壶、35 张魔物狩人。全部槽位留空并禁止魔物狩人后，初始手牌为 5 张强欲之壶，35 张禁用卡全部留在牌组；随后发动强欲之壶正常抽到 2 张魔物狩人。
- AI 开关关闭时，强欲之壶正常抽 2 张；开启时，对手的灰流丽真实发动、支付丢弃费用并使抽卡效果未生效。重试恢复对手的灰流丽及初始状态，可再次响应；进入对手回合后实际通常召唤魔物狩人并结束回合。
- 正式方案在重启后完整一致；修改源牌组、删除其他方案和源牌组后，原正式方案完整内容仍保持一致。旧训练历史、冻结 YDK 和原始 JSONL 可访问。
- 原生场地的父窗口、尺寸、125% 缩放、页面切换、合成层遮挡、正常启动拒绝内部测试接口，以及退出时进程和服务端口释放均通过检查。
- 原生补丁应用、VS 2026 x64 编译、JavaScript 语法检查与 Git 差异空白检查通过。Windows 1.1.0 目录及 ZIP 包由 `npm run build` 生成。

本次修复了验收中发现的窄窗口缩放导致原生区域超出主窗口的问题。后台 CDP 截图曾超时，验收改用 Electron 自身的 `capturePage(stayHidden: true)`，预热后捕获渲染器；原生图像仍取自引擎渲染驱动。截图与结果保存在 `.local/evidence/electron-development/` 和 `.local/evidence/electron-packaged/`。

基础 AI 仅覆盖文档中的固定干扰配置，没有复杂策略或战斗决策。测试中的数值边界使用合成牌组；未声称逐一实测所有真实卡组及任意复杂卡效。以下保留旧版本的历史验收记录。

## 实际运行

在现有工作副本完成，测试构筑为专门建立的自由练习构筑，不覆盖用户已有 YDK。

| 验收项 | 结果 |
| --- | --- |
| 卡牌浏览／编辑 | 搜索强欲之壶和青眼究极龙，显示本地卡图与效果文本；添加、移除卡牌并编辑主／额外／副卡组 |
| 保存与重开 | 保存主 40、额外 1、副 1，切换构筑后重新打开核对 |
| 真实载入 | 原生 `DeckManager` 载入冻结 YDK，核心开始快照的主／额外卡号及张数与所选构筑一致 |
| 手动展开 | 在魔法陷阱区 1 发动强欲之壶，实际抽 2 张；通常召唤魔物狩人到怪兽区 1 |
| 最终场面 | 画面与核心快照同为怪兽区 1 张、墓地强欲之壶 1 张、手牌 5 张、卡组 33 张、额外 1 张 |
| 手动结束 | 点击原生“结束训练”生成正常完成报告；摘要包含效果抽卡与通常召唤两步 |
| 规则抽卡过滤 | 第二轮在魔法陷阱区 3 发动强欲之壶后结束回合；占位方跳过，己方下一回合正常抽 1 张。原始记录包含起手 5、效果 2、规则 1，摘要仍只有一条效果步骤，效果抽卡统计为 2 |
| 无 AI 对手 | 训练期间 Bot／WindBot 进程数量为 0，模拟器 TCP 连接数量为 0；对手保持空场，仅结束回合／跳过可选响应 |
| 中断 | 关闭第二轮模拟器窗口后报告标为“中断 / client_closed”，保留已有效果步骤和最后区域状态 |
| 重启与历史 | 重启 Python 服务并重新打开历史，已完成记录仍可见；“强欲之壶抽卡 → 通常召唤”摘要与原始依据可正常切换 |
| 旧记录更新 | 旧版真实记录中的 5 次强欲之壶发动生成 5 条摘要；原始选择、阶段和常规送墓事件仍可从依据查阅 |
| 窄窗口 | 检查窄窗口构筑选择器、打开构筑和开始训练；避免侧栏折叠后无法选择构筑 |

实际运行发现并修正：核心 `effect.id` 在发动后重排导致结果未关联。新增会话内 Lua 效果句柄，新客户端记录中发动与抽卡的句柄保持一致；旧中间版本记录通过对应连锁结算完成快照核对重排编号。另修复了原生按钮仍显示“投降”和历史列表重复刷新导致焦点丢失的问题。

## 自动化与构建

- `python -m unittest discover -s tests -q`：46 项通过。覆盖效果原文／序号、发动后费用窗口、多张费用聚合、牌底排序和实例位置维护、连接／同调／超量／融合类型与素材、复活不误称融合召唤、效果内召唤／盖放去重、LP 费用与连锁归属、区间关闭／记录缺口／付款方和快照不匹配、米迦埃莉丝描述索引到效果③的对应等检查。
- `node --check src/trainer/web/app.js`、`node --check src/trainer/web/report-view.js`：通过。
- `python scripts/apply_lite.py` 与 `scripts/build.ps1`：在已准备的 VS 2026 x64 工作副本中重新编译、部署通过；未纳入音频与录像播放实现。
- `python scripts/fetch_sources.py --verify-only`：固定客户端和全部编译依赖归档的 SHA-256 校验通过。
- 测试中发现 SQLite 连接上下文不会自动关闭文件，已显式关闭并重新验证。

## 效果原文、素材与排序改进

- 将已有白龙真实记录重新生成：抵价购物先列丢弃青眼亚白龙的费用，再列抽 2 张；贤士标为效果①且只显示该段原文；贤士素材送墓与连接栗子球召唤合成一项。
- 将已有黄金国真实记录重新生成：金满而谦虚之壶的 6 张里侧除外费用合成一项，翻开 6 张、拿取红化血染之黄金国永生药、剩余 5 张按顺序放回牌底分别展示；黄金卿效果②列出送墓费用、回手和从手卡特殊召唤，保留与连锁 2 的正确归属。
- 浏览器中核对上述原文与处理、加粗卡名、历史报告及主／额外怪兽区分组。
- 使用新客户端实际运行“通常召唤贤士 → 发动效果①检索白石 → 选择贤士作为连接素材 → 在额外怪兽区 1 连接召唤连接栗子球 → 手动结束”。报告为三步；核心 `summon_info` 确认为连接召唤，素材实例对应实际贤士。终场为主怪兽区空、额外怪兽区 1 张连接栗子球、墓地 1 张贤士、手卡 5 张、卡组 34 张、额外卡组空。

连接召唤已重新桌面实测；融合／同调／超量类型与素材关联使用合成协议验证，尚未逐一卡组实战测试。全新环境完整下载、解包和全量构建未重跑。效果编号只在可验证时提供，其他多效果卡、持续、替代或复制效果信息不足时保留独立变化或未知状态。

原始安装、工作副本已有构筑／单人脚本／录像和修改前训练文件的最终校验结果保存于 `.local/evidence/final-verification.json`，该文件保留本地，不进入 Git。
本次原文／素材／排序改进的校验结果另存 `.local/evidence/effect-detail-verification.json`，不覆盖此前验收依据。

## LP 费用与米迦埃莉丝效果③

用已有救祓少女真实记录重新生成并核对报告：米迦埃莉丝的检索显示效果③原文；救祓少女和平问候的实际过程依次为支付 800 LP、马尔法加入手卡、从手卡特殊召唤马尔法到主怪兽区 1，LP 不再单独占据展开步骤。金额取自原始 `MSG_PAY_LPCOST`，连锁和效果身份来自同一发动费用区间的快照。此次调整为报告解析与展示，未改动或重编译模拟器内核，也未另开对局重测。
浏览器页面文本核对通过；截图采集工具超时，本次未进行新截图比对。已有日志、构筑快照、元数据及旧版报告的校验和保持不变。

## MDPro3 参考卡组编辑页

参考用户提供的 MDPro3 截图重排为“左侧详情／中间构筑／右侧搜索”，保留当前应用的数据来源、YDK、保存备份和训练流程。未推断截图所示禁限表、收藏和关联卡牌的内部实现。

- 在浏览器实际打开已有构筑，主 40／额外 15／副 15 以独立卡图同时展示。1280×720 下三个分区全部落在构筑可视区，卡图加载正常；另检查 1600×932 的分区位置与卡片尺寸。
- 实测单击详情、双击搜索结果加入主卡组、右键移除一张重复卡、左侧按分区加减、按钮撤销、`Ctrl+Z` 撤销、排序与未保存状态。副卡组达到 15 张时加卡按钮禁用；主卡组移除同名卡没有影响副卡组副本。
- 新建专用本地验收副本，保存后刷新页面重新打开，主 41／额外 15／副 15 与两处分区内的测试卡保持一致。原有构筑没有覆盖，测试构筑和自动保存备份保留在 `.local/`。
- 实测额外卡组筛选、卡名搜索、空结果提示和分页；900×900、390×844 下无页面横向溢出，打开、新建、保存和搜索控件可达。
- 重新打开已有训练历史，报告及原始事件切换入口正常。此次没有修改或重编译原生模拟器，也没有另开训练重测结算。
- `node --test tests/deck_editor.test.cjs`：11 项通过，覆盖重复卡分区与顺序、撤销、容量和衍生物限制、排序、迟到请求、新建时的选择器保护、卡片请求去重、保存冲突及副本保存。该测试使用合成数据与 DOM 桩，不能代替视觉验收。
- 现有 Python 46 项测试及两个浏览器脚本的语法检查通过。

浏览器截图 API 本次仍不可用；上述界面结论来自实际浏览器交互、DOM、卡图加载状态及布局尺寸检查，未完成逐像素截图比对。修改前后的原有构筑与训练源文件哈希结果保存在 `.local/evidence/editor-ui-preservation.json`。

## YDK 批量导入

- 通过浏览器实际文件选择器读取本地 YDK，预览显示主 40／额外 15／副 15 及分区卡牌清单；点击导入后一次载入，进入未保存的新构筑。
- 保存验收构筑、刷新页面并重新打开，逐项比较三个分区的完整卡号数组，重复卡及原始顺序完全一致。导入源文件与原有 49 个构筑／训练源文件的哈希保持不变，依据存于 `.local/evidence/ydk-import-preservation.json`。
- 实测包含未知卡号的错误文件：提示具体行号及卡号，禁止导入，当前主卡组仍为 40 张。取消后当前构筑不变。
- 实测粘贴完整 YDK 原文，主／额外／副各识别 1 张；提示主卡组不足 40 张，可作为草稿继续编辑。390×844 下导入面板无横向溢出，底部操作按钮可见。
- `node --test tests/deck_editor.test.cjs`：24 项通过。新增覆盖 BOM／CRLF、重复卡和分区、未知卡号、非法行和重复分区、容量与衍生物、脚本缺失提醒、连接失败、预览不改当前卡组、过期预览、同名及 Windows 保留名称、未保存修改取消保护、文件读取与拖放事件。
- 原有 Python 46 项测试以及两个浏览器脚本语法检查通过。系统文件拖拽用合成事件验证，未进行系统文件管理器的真实拖拽测试；本次未重新采集截图。

解析在页面内完成，卡牌校验与保存复用现有本地接口。服务重载命令被执行策略拒绝，未重启服务；最终版本已在原服务上刷新验证，无新增后端接口或原生构建要求。验收 YDK、新建测试构筑和依据均保留在已忽略的 `.local/` 中。

## Electron 桌面与嵌入式场地

当前桌面版由 Electron 主进程启动固定版本的嵌入式 Python 服务，并用独立用户数据目录启动工作副本引擎。YGOPro 在创建时使用 Electron 主窗口作为 Win32 父窗口，保留原生场地控件和规则处理，场地位于“训练场地”页面内。后台仍为独立原生进程，由本次服务的 Windows Job Object 管理。

按用户要求，后续验收改为后台实例：通过引擎自己的命中检测、Irrlicht 事件及渲染截图完成操作，不移动系统鼠标、不发送全局按键。验收实例使用固定种子并在原始日志标记 `test_control: true`，正常训练继续使用随机种子，内部测试接口默认关闭。

| 验收项 | 实际结果 |
| --- | --- |
| 开发版 | `npm run test:desktop` 通过，真实引擎完成强欲之壶抽 2 张、魔物狩人通常召唤，生成 2 步报告 |
| 打包版 | `npm run test:packaged` 通过，同样完成真实效果、召唤和报告；PATH 仅保留 Windows System32，未使用外部 Python、Node 或开发服务器 |
| 场地嵌入 | 检查实际父窗口、WS_CHILD、无独立标题栏和场地区域坐标；预留区域的子窗口命中测试由原生场地接收；切换编辑／训练／报告时隐藏与恢复 |
| 缩放 | 打包版改变主窗口尺寸并检查原生窗口与渲染尺寸；125% 页面缩放下区域宽度、纵向位置对应正确 |
| 编辑与抽屉 | YDK 文件导入保留主 40／额外 1／副 1 及完整顺序；搜索、加卡、撤销、保存重开通过；抽屉默认收起，开合保留构筑 |
| 删除构筑 | 取消删除不改变文件；确认删除后列表移除并保存 YDK／元数据备份；该构筑原有训练报告仍能读取 |
| 效果文本 | 已知和未知编号均展示完整冻结文本；标题无“编号未知”，实际结果单列；现有旧报告也使用该显示方式 |
| 原始依据 | 原始事件切换与 JSONL 查看通过；JSONL 在受隔离的 Electron 附属窗口打开 |
| 重启与退出 | 重新启动读取相同构筑和完整报告；训练开始后关闭主窗口，写入 `client_closed` 中断报告，释放原生进程、Python 服务和端口 |
| 生产数据 | 从开发数据副本备份迁入 6 个我的构筑、12 个原有副本构筑、13 条训练；93 个迁移文件在生产版启动、查看和退出后哈希一致 |
| 默认安全边界 | 正常启动的内部测试接口拒绝调用；重复启动复用原服务；后台验证不显示或激活窗口 |

自动化检查：Python 57 项、JavaScript 28 项通过，相关脚本语法检查通过。MSVC x64 原生重建、固定源码归档 SHA-256 校验与 Electron Windows ZIP 构建通过。ZIP CRC 校验通过；资源白名单含 28,888 个文件，不含个人构筑、会话、旧配置、日志、Bot/WindBot 或录像；默认配置与源码模板一致。

过程中修复了首次准备等待时间过短、打包重复覆盖正在运行的 Python、关闭期间原生动画线程未唤醒导致缺少结束标记，以及 Python 标准输入后台读取在解释器结束时触发错误的问题。另修正原生子窗口可能被 Chromium 层挡住的问题，布局更新时将场地放在父窗口内的最上层，同时保持不激活窗口。缩放验收曾读取窗口调整前的旧帧，改为等待实际视口和原生渲染尺寸同步后复测通过。

原始 YGO 的 29,496 个文件内容及 SHA-256 与原先基线一致。本次严格时间戳检查发现 `system.conf` 时间戳不同，但文件字节和哈希未变化；保留该时间戳，没有回写原始目录。依据在 `.local/evidence/electron-original-preservation.json`。

迁移证据、完整流程结果及界面／场地截图位于 `.local/evidence/desktop-data-migration.json`、`production-profile-result.json`、`electron-development/`、`electron-packaged/`，均留在本机。迁移备份位于 `%LOCALAPPDATA%/YGOTrainer/migration-backups/`。

限制：只在当前 Windows 11 x64 主机验证，未使用全新 Windows 10/11 虚拟机或其他显卡；未验证跨显示器的不同系统 DPI。按用户要求未再用系统输入执行物理键鼠／Alt+F4 验收，主流程通过内部事件测试。原生子窗口不属于 Chromium DOM，场地图像从引擎渲染器获取；代码签名与安装器未提供，交付为完整解压后启动的 ZIP 包。原有复杂卡效、融合／同调／超量等未逐一卡组实战的限制保持不变。

## 1.0.1：精简界面与响应延迟

- 用户报告删除后连续操作迟滞。后台原删除／打开数据路径约 41／43 ms，未复现同样的长时间后台请求；确认框改为页内弹窗后，发现依赖 `close` 事件仍可能等待后台窗口的下一帧，测试中达到约 1.8 s。改为从提交／取消事件直接完成确认，随后恢复选择器焦点。
- `npm run test:deletion`：确认窗口打开期间定时器继续执行；Esc 取消保留构筑；确认删除后立即打开 39 种卡牌的下一构筑。开发版删除约 27 ms、下一构筑约 37 ms；打包版约 28／48 ms。未使用系统确认框。测量为本机单次回归，非所有机器的延迟保证。
- 用户运行记录显示：引擎载入约 416 ms，首次可操作阶段约 3542 ms，中间主要是发牌与阶段提示动画。1.0.1 跳过开场和阶段过场的帧等待，完整处理、记录核心消息；基本测试构筑的开发版首次可操作约 508 ms／首帧 579 ms，打包版约 491／561 ms。两个测量使用不同构筑，不作为严格同输入性能基准。
- 首帧生成后清除准备提示，不再让已经启动的场地一直保留“正在准备”。移除对手状态栏、上半场底图、辅助／系统标签、时点切换、洗切手卡和重复结束按钮；保留自己的 LP、卡片信息、手牌／场地、阶段与效果选择。规则用的最小占位留在内部；已有日志和报告不删除。
- Python 57 项、JavaScript 29 项通过；开发和打包版本均完成抽 2 张、通常召唤、2 步报告、缩放、重启和退出清理。新增 `desktop/deletion-smoke.cjs` 用独立测试构筑检验确认、取消、焦点和连续操作。
- 1.0.1 输出到 `release/1.0.1/`，保留正在使用的 1.0.0 目录，避免覆盖运行中的程序。用户数据继续使用同一独立目录。升级只更新清单中改变的资源，未改变的卡图和脚本无需重新全量计算哈希。

## 1.0.2：训练场地空白

用户截图显示“训练中”，但场地只有网页背景。对旧 1.0.1 包使用隔离数据运行后，原生引擎首帧与命中检查均通过，窗口树却包含覆盖整个网页区域的 `Intermediate D3D Window`，带有 `WS_EX_LAYERED` 和 `WS_EX_TRANSPARENT`。这说明此前的内部场地截图和命中检查不足以排除网页合成层遮挡。Windows 的分层窗口可将鼠标输入透传给下面的窗口，因此输入命中不能证明画面未被覆盖，见 [Microsoft 分层窗口说明](https://learn.microsoft.com/en-us/windows/win32/winmsg/window-features#layered-windows)。

- 启动主窗口前设置 `disable-direct-composition`，改用兼容普通 OpenGL 子窗口的 HWND 合成。对照运行中，D3D 分层窗口消失，主窗口恢复普通重定向；`gpu_compositing`、OpenGL 和栅格化仍为启用状态。原生引擎、规则和记录格式没有修改或重编译。
- 场地状态额外检查与原生区域重叠的分层窗口；只有首帧、显示样式、输入命中和合成兼容均通过才关闭准备提示。持续失败时显示结束训练并重启的提示。即使尺寸没有变化，后台同步也会修复被其他子窗口改变的输入层级。顶部两个训练按钮改用深色背景和浅色文字。
- `npm test`：Python 60 项、JavaScript 32 项通过。新增隐藏 Win32 窗口用例重现“首帧与命中均成功但仍存在分层遮挡”，并验证隐藏／不相交窗口、层级恢复、加载失败提示与过期会话响应。
- `npm run test:desktop`、`npm run test:packaged` 均通过。真实引擎完成强欲之壶抽 2 张、魔物狩人通常召唤和 2 步报告；改变窗口大小、125% 页面缩放、切换页面后均无分层窗口遮挡。异步宿主检查显式轮询服务结果，避免尺寸更新尚未完成就结束等待。
- 两种版本都额外以未启用测试控制的方式重启并开始训练：合成检查通过，日志标记 `test_control: false`，内部截图／输入接口返回拒绝；固定种子仍只用于明确启用测试控制的实例。重启保留构筑和报告，关闭训练生成中断报告并释放进程和端口，删除操作保留恢复备份。
- `npm run build:dir` 通过；核对包内主进程、版本、Python 宿主与网页文件和当前源码逐字节一致。应用输出到独立的 `release/1.0.2/`，保留旧版目录；本次没有打开或修改正式用户数据目录。

对照窗口信息及原生图像保存在 `.local/evidence/render-legacy.*`、`render-composition.*` 和 `render-baseline.*`；最终流程结果、宿主状态及原生场地图像保存在 `electron-development/` 与 `electron-packaged/`。`training-shell.png` 只包含网页渲染器，用于检查按钮和页面；`embedded-board.png` 来自原生渲染驱动，二者没有拼接为桌面截图。

本轮全部窗口验收均在后台隔离实例中执行，没有移动系统鼠标、发送全局按键或接管其他窗口。没有进行前台物理键鼠验收，也没有复测其他显卡、Windows 版本或跨显示器 DPI。之前的“内部截图通过”不作为整窗合成已经可见的单独证据。
