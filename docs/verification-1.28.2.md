# 1.28.2 验证记录

## 修复范围

- 手动与自动决斗教程一次展示当前 Step；宽度自适应，长内容在步骤内滚动。卡图立即加载，竖置 120 × 175 px、横置 175 × 120 px，方向变化不缩小。步骤导航、分支、快捷键、场面查看和高度调节保留。
- 效果处理中的展示卡牌有独立卡图、名称和区域。新报告保留确认卡牌事件；旧冻结方案按同一连锁的原始事件补充视图，一图流与临时步骤同步展示。
- 自适应搜索从抽卡／准备阶段进入来源的主要阶段起点，不误接来源中途的放弃响应。
- 完整卡牌／效果标识一致时，支持“选择连锁”与“是否发动效果”间的肯定选择重绑。精确模式、未知身份、效果信息不完整、脚本标识冲突和歧义选项仍拒绝近似。

## 已完成验证

- `npm test`：合并当前主分支后 374 项 Python 检查和 203 项 JavaScript 检查通过，覆盖展示事件归属、旧方案不改写、导出、临时步骤、窗口重绑及否定／歧义边界。
- `npm run test:desktop` / `npm run test:packaged`：隔离验收版本各 68 项完整桌面检查通过，错误列表为空，`globalInput=false`。另执行原生引擎专项，并对合并后的最终发布包复核自动工作区、大卡图完整加载与真实路线生成，均通过。
- 自动工作区桌面验收通过：单步布局、960 px 窄窗口、后续长步骤不影响当前卡图／文字、当前长步骤滚动、分支和快捷键。
- 本地复现样例的 5 张起手、5 个冻结来源，在开发版与独立发布包中均得到 2 条 `engine_verified` 候选；终场标记均完整匹配 4 张，没有未确认随机结果。搜索仍达到本轮预算，不宣称穷尽所有路线。
- 开发版及发布包截图核对：同调超车的展示对象有卡图与名称；只显示当前 Step。源方案文件逐一比较 SHA-256，保持不变。
- 发布包构建成功；规则与搜索源码同隔离验收目录逐文件核对。

## 数据与边界

验收运行于隐藏 Electron 和隔离数据目录，通过应用内部接口操作和截图，不发送全局按键或移动系统鼠标。牌组、原始记录、个人来源文件、完整计算结果、截图和发布包只保留本地，不进入 Git。本次没有修改真实对局输入或将临时结果自动保存为正式方案。

原路线条件筛选与逐步规则验证仍是两层检查；下方匹配到已保存方案，不等于上方已经验证整条来源可在本局原样重放。搜索会保留原路线失败原因及已验证的替代候选。
