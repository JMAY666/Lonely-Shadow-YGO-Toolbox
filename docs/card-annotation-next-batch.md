# 第四批补卡提示词（已完成，保留历史范围）

2026-09-25 的 1.47.4 标注扩充已经覆盖下列 12 张优先卡，并继续完成相剑、驱魔姐妹、转生炎兽、天威整系缺口；实际新增为 100 张。下文是当时的任务提示词，不再作为当前待办或数量上限。请以[本轮实际清单](card-annotation-batch-2026-09-25.csv)、[交接指南](card-annotation-agent-guide.md)和[1.47.4 验证记录](verification-1.47.4.md)为当前状态依据。

下面的内容可直接复制给下一个对话执行。

请在 `E:\AI项目\游戏王工具箱` 继续补充卡片效果结构化标注。本轮目标是补齐杀手旋律／杀手级调整曲系列的 8 张缺口，再补 4 张实战辅助相关支援卡；直接完成资料核对、标注、测试、文档和按项目规则审核发布。

先读当前 `AGENTS.md`、`docs/card-annotation-system.md`、`docs/card-annotation-agent-guide.md`、`docs/verification-1.47.3.md`，检查当前 Git 根目录、分支、远程、工作区和暂存区。1.47.3 已修复上一轮的个人修正版本隔离、费用说明、TAG、快速效果标志与区域筛选问题；不要重复执行旧审查提示词或撤销这些修复。后续已有变化以当前仓库为准，保留其他人的修改。

当前基准为 108 张效果标注；`AUDITED_EFFECTS` 和 `staples.json` 所含卡号已经覆盖，不重复计为新增。20 系列／35 张代表卡的展示资料是独立口径，不能计入效果标注完成度。

本批清单如下，名称为本地译名，卡片身份以整数卡号为准：

| 卡号 | 本地卡名 |
| --- | --- |
| 15665977 | 杀手级调整曲·红印鉴唱片师 |
| 39576656 | 杀手级调整曲·噼啪削波手 |
| 40847034 | 杀手级调整曲播放列表 |
| 41069676 | 杀手级调整曲响度战争 |
| 43904702 | 杀手级调整曲·削波手 |
| 65961304 | 杀手级调整曲 B2B |
| 78058681 | 杀手级调整曲同调 |
| 88170262 | 杀手级调整曲·再混音手 |
| 65681983 | 抹杀之指名者 |
| 61049315 | 自然蔷薇鞭 |
| 99243014 | 同调超车 |
| 44362883 | 烙印融合 |

执行要求：

1. 先用实际运行卡库复核上述卡号、`name/type/desc`、系列归属和已有覆盖。已经完成的条目先复核后跳过；不要覆盖更准确的资料或重复统计。本批全部完成且没有并行变更时，预计覆盖由 108 增至 120，`set:1d5` 由 6/14 增至 14/14；这是当前基准的预计值，不能为了凑数把不确定条目标成已核对。
2. 逐卡访问 KONAMI 官方 OCG 卡片页与 FAQ，记录 URL、地区、日期及来源引用，原始来源快照保存在 `.local/`。官方站内的用户牌组评论不作为裁定；只有 TCG 或证据不足时明确待核对，不推断 OCG 规则。数字卡号、官方 `cid`、系列 setcode 和异画卡号分别处理，不改写卡库译名。
3. 修改 `src/trainer/card-annotations.json` 时保留旧条目与稳定 ID。完整保存本地 `frozen_text`，用 `card_annotations.digest()` 计算指纹、`segments(desc,type)` 生成分段；覆盖无编号规则段、全部编号效果、素材手续、召唤限制、共享次数关系及灵摆双区（如适用），不能手写错误的效果编号映射。
4. 逐效果分清类别、发动区域／时点／条件、费用、对象、数量、处理来源与去向、执行者和受影响者、次数、持续时间、顺序、互斥分支及自锁。特别核对送墓费用与丢弃费用、发动次数与使用次数、取对象与处理时选择、发动无效与效果无效、自锁是否回溯本回合。条件化的快速效果须保留条件；诱发效果能在对方回合触发不代表它属于快速效果。
5. TAG 概括明确能力，不能把费用或响应条件误当处理。复用已有词表；确需新增取值时先补定义、正反例、兼容规则和适用校验。当前校验器会检查部分 TAG 与处理、效果类别与快速标志的一致性，失败要核对资料，不得删掉检查。来源区域应明确登记；仅有“场上”不能据此声称能处理某个特定子区域。同一效果中的互斥分支不等于一次发动能全部执行。
6. 完成人工资料核对后才可设 `reviewed/manual`。自动初稿、缺段或未解决裁定保留草稿并说明缺失证据。引擎脚本映射不等于完整规则验证，静态搜索命中不等于当前局面可发动。本批只维护静态卡牌知识，不启动封存的 AI 学习计划或无关重构。
7. 不写用户运行目录，不改个人 TAG、备注、牌组、方案和备份，不把旧个人标注重新绑定到新卡文。为本批关键机制增加有意义的查询正反例，覆盖费用与处理、互斥选择、限制和区域差异，不能只断言新增数量。

完成后运行：

```powershell
python scripts/check_card_annotations.py --runtime .local/YGOPro-Lite
python scripts/check_annotation_series.py --runtime .local/YGOPro-Lite
python -m unittest discover -s tests -p 'test_card_*.py' -q
node --test tests/card_annotations_view.test.cjs
npm run test:desktop -- --annotations-only
```

若改变公共服务或界面行为，补跑相应项目检查；发布桌面产物时先构建与当前源码一致的包，再执行 `npm run test:packaged -- --annotations-only`，不能拿旧包或历史验收代替本次结果。桌面测试使用唯一 `YGO_TEST_RUN`、后台 Electron 和隔离数据，不移动系统鼠标，不发全局按键。

同步 README、体系规范和交接指南的实际数量、行为与验证记录。按 AGENTS.md 的既有授权审核必要文件、完整待推送历史及隐私范围，明确路径暂存、提交和推送到核实的现有远程／目标分支，再检查 GitHub About。仅清理确认无用的可重建材料，保留个人数据、来源快照、正式证据和必要备份。

最终报告实际新增／跳过的卡号与原因、官方来源、覆盖增量、未解决裁定、测试与打包结果、提交哈希和推送目标。若证据不足或检查失败，如实说明，不夸大完成度。
