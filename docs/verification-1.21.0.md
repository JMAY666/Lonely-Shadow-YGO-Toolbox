# 1.21.0 验收记录

本次将自动选择从示例预览接入真实 YGOPro 进程读取与本地持久化保存。

- 单元检查：`npm test` 通过，239 项 Python、167 项 JavaScript。
- 首轮真实进程验收：在 YGOPro 编辑器中读到主卡组 40 张、额外 15 张、副卡组 5 张；三分区缩略图、卡牌详情和逐副本标记、真实 TAG 主副调整、命名保存与落盘回读一致性通过。
- 实时验收使用独立 `.local/ygopro-live-*` 数据目录；进程路径、个人卡组、截图和测试日志只保留本地，不纳入 Git。
- 原先打开的是只读的人机卡组，随后用户切换可编辑卡组并重建基准：40／15／0 张。
- 第二轮真实测试：用户替换 3 张主卡牌后准确读取；同名覆盖后 ID 不变、revision 更新、列表只有一个条目，回读与新卡组完全一致，并核对到与原 revision 相符的备份。
- 第三轮真实测试：用户切换另一套卡组后读到 40／15／15 张，保存为独立的第二个卡组 ID，第一套内容保持不变。
- `npm run test:desktop -- --duel-only`：开发版决斗专项 5 个检查组通过，前端错误为 0。
- `npm run test:packaged -- --duel-only`：打包版决斗专项 5 个检查组通过，前端错误为 0；EXE 名称、说明及图标检查通过。
- `npm run build` 成功生成 1.21.0；本次应用源码与打包资源逐文件哈希一致。
- 捕捉弹窗及识别页均显示“先从主菜单打开编辑卡组并停留”的提示。实际离开编辑页面时读取被拒绝；重新载入工具箱页面后，两副已保存测试卡组保持不变。

最初全量桌面测试在“点击平台直接进入卡组页”的旧断言处停止；该断言已按捕捉弹窗流程修正，随后完成上述开发／打包决斗专项复测。本次不宣称全量桌面回归完成。非本应用窗口未被操作，游戏进程没有写入或注入。

后续另外探测了进行中对局的完整构筑与我方手牌；证据和地址已保留在 [对局数据读取路径](ygopro-live-state-research.md)，此能力尚未接入 1.21.0 界面。

适配范围与限制见 [BO1 决斗说明](duel.md)。本版本只支持已核实可执行文件指纹的 YGOPro 1.036.2 x64；未验证其他构建。TAG 自动推荐来自本地规则，自动决斗控制尚未接入。
