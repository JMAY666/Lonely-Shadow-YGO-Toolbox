# 1.29.0 验证记录

## 范围

BO1 后攻执行计划 P1.1：独立的当前资源与观察历史、手动入口及已适配开局的衔接、历史回看和人工恢复。交康提示、规则状态重建与后攻路线仍未实现，不能把本阶段称为第一期完整闭环。

## 已执行检查

- `npm test`：最终一次为 389 项 Python、211 项 JavaScript 通过；随后补充卡库缓存隔离测试，`node --test tests/*.test.cjs` 的 212 项全部通过。新增观察状态测试 15 项、前端模型与隔离测试 5 项。审核发现的后攻历史误触发先攻推演风险及冻结卡文污染共享卡库缓存风险均已修复，并分别验证先攻原行为与当前卡库内容保持。
- `npm run test:desktop -- --second-only`、`npm run test:packaged -- --second-only`：通过。五张起手使用一张手坑再抽一张后，当前仍为五张且原始快照不变；实际不响应、未知盖卡、旧版本请求拒绝、历史入口、人工恢复核对、900px 布局、持久化及先攻入口检查通过。
- `npm run test:desktop -- --smart-only`：四个平台模拟接口通过，覆盖后攻进入独立记录、先攻进入原方案页、取消／跨局与迟到结果。不是外部客户端真人验收。
- `npm run test:desktop -- --duel-only`：通过。先后攻与起手捕获页面、手动先攻教程、原始方案保留、分支、鼠标及内部快捷键控制、完整 Step 展示及原分隔栏范围回归。
- `npm run test:packaged -- --automatic-workspace-only`：通过。真实本地方案匹配／选择接口、独立自动教程、模式往返、排序／收藏、分支、快捷键及 900–1440px 宽度和 650–970px 高度下的图示与分隔栏回归。
- `node node_modules/electron-builder/cli.js --win --x64 --dir`：1.29.0 目录包构建通过；沿用已保留的原生资源归档，不重编引擎。品牌检查和新增应用文件 SHA-256 对应核对通过。

最终后攻证据位于本地 `.local/evidence/electron-development-second-bo1-second-final/second-result.json`、`electron-packaged-second-bo1-second-p11/second-result.json`。智能入口、先攻及自动教程证据分别位于带 `bo1-second-smart`、`bo1-second-regression`、`bo1-second-auto` 标签的隔离目录。中间失败保留本地：新静态资源白名单漏接已修复；窄窗口测试改为现有 900px 最小宽度，未缩减调节范围；旧文字断言同步更新并保留行为检查；测试导入源改为隔离空目录，避免依赖不存在的旧开发运行副本。

## 未验证范围

本阶段未修改原生规则，没有运行新增后攻规则／斩杀专项。没有交康推荐、后攻路线或完整局面自动跟踪；这些仍属于后续执行项。四平台新入口为模拟接口验收，MD 普通 PvP、YGOPRO2／MDPRO3 真实后攻及客户端更新后的支持仍须单独验证。没有完整核对当前 MD 卡池和禁限表。

后台验收只操作本应用隔离窗口；未发送系统鼠标或全局按键。测试牌组、截图、JSON／JSONL、日志和包保留在忽略目录，正式方案及个人数据不纳入本次修改。

提交前清理 6 个本轮隔离测试运行目录中由不可变资源归档管理、逐文件哈希一致且无运行进程使用的 173,322 个可重建资源文件，共 7,673,603,526 字节（约 7.15 GiB）。保留全部 JSON／JSONL、观察记录、截图与正式证据、设置、备份、资源清单和发布包；未删除个人数据或未知文件。清理依据和结果留在 `.local/evidence/bo1-second-cleanup-*.json`。
