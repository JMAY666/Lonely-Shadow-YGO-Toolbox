# 1.37.0 TAG 关联卡片验证记录

本次增加 TAG 关联候选、单卡关联浏览、成员来源显示、手动排除恢复和分页。MDPro3 的源码分析、工具箱采用的关系范围及使用方法见 [TAG 关联卡片](tag-relations.md)。原有系列编号、卡组 TAG 注释、手动方案分类与识别阈值继续沿用。

## 已执行

- `npm test`：385 项 Python、207 项 JavaScript 全部通过。新增 10 项关联测试覆盖精确名称、括号／引号、双向关系、无前缀推断、无递归扩散、跨系列隔离、signed setcode 与子系列、脚本文字及注释隔离、动态表达式跳过、分页、排除／恢复、资源重载、保存失败与旧数据保留。
- `npm run test:desktop -- --tags-only`：开发版最终代码通过。先前一轮通过后增加标签读取时的编辑保护、归属来源修正和新标签保存后的列表定位，再对最终代码复测。
- `npm run test:packaged -- --tags-only`：1.37.0 打包程序通过同样的真实卡库流程。打包 EXE 的产品名、说明和七个源图标帧核对通过；启动只使用内置 Python，开发环境工具不在其 PATH 中。
- 两个桌面模式均覆盖：“传说的白石”按“青眼白龙”的精确引用成为候选；只读浏览不更改 TAG；确认加入后同时参与卡组副本浓度和方案用卡浓度；保存失败保留草稿；跨页面草稿保留；系列成员排除及恢复；重载后持久化；卡图悬停；“烙印融合”与“阿不思的落胤”的双向查询；按关系类型筛选；迟到结果不覆盖新查询；失败后可重新查找。
- 布局检查：TAG 页面在 900×700、1440×920 下无横向溢出，截图由隐藏的本应用渲染器获取。启动标题栏同时检查 900／1280／1600 宽度和 125% 缩放。未移动系统鼠标或向其他窗口发送全局按键。

桌面数据在本轮独立的 `.local/desktop-check-*-tags-relations-*` 中生成，使用空导入目录，未导入个人牌组。方案浓度验证使用明确标记的合成路线；其文件在关联发现与 TAG 修改前后逐字节一致。这不代表新增路线经过原生规则引擎验证。

## 构建与本地资料

使用 `electron-builder` 的目录包和 Windows x64 ZIP 构建，输出至独立的 `release/1.37.0/`。复用现有离线 Python、未改变的原生运行资源归档及对应源码／许可材料；本次不需要重新构建原生引擎。没有使用缺少原始工作副本输入的 `desktop:prepare` 来重新生成原生资源。

发布检查对打包 `resources/trainer` 的文件集合与源码逐项比对，确认新增关联模块、网页资源及版本号进入包中；不把测试产生的 Python 缓存、测试用户目录、研究下载、牌组、备份或个人记录打入包内。MDPro3 研究源码仅保存在本地忽略目录，没有随应用或 Git 分发。

沿用 `.local/`、`release/` 和 Python 缓存忽略规则；检查结束后，仅清理本轮已退出验收实例中与资源清单哈希一致、可从离线包还原的副本。原始 JSON／JSONL、截图、报告、用户设置、TAG 备份、旧发行包及其他任务资料保留。具体文件校验和清理结果留在本地 `.local/evidence/`。

## 未验证范围

没有重新进行真人联机、多平台开局识别或全部原生决斗专项；本次未更改其引擎、内存读取或输入逻辑。关联不证明合法配合、效果适用性或战略价值。动态 Lua 条件、宽泛种族／等级关联和 MDPro3 的整张预生成表没有移植，结果可能不完整；这些限制已在使用说明中明确。
