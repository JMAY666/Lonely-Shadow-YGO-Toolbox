# P3 验证集教师选择：预登记

2026-09-21，在本轮任何验证家族执行前登记。P2 指纹、200 家族、120/40/40 划分、目标和失败分母保持不变。训练标定报告 `report-20260921-113043` 的时间与存储门槛均通过，才允许进入本步骤。

本轮只评估一个固定候选 `T0-resource-v7`，与冻结 B1 配对；不根据验证中间结果修改教师。只运行 P2 中全部 40 个 `validation` 家族，每类 8 个，按冻结顺序分 8 批，每批 5 个。40 个 holdout 家族继续封存。B2 的范围仍限于已排除起手的五场公开示范精确匹配，不宣称覆盖产品全部模块；本轮不增加 B0、LLM 或学生对照。

每个新家族在 B1 执行前保存一次完整扩展条件；T0、失败重试和重放复用同一条件。双方都使用开启测试动画加速的同一原生构建。目标只按首回合结束的原生日志判定。超时、不支持、循环和未达标均留在全部 40 个家族的分母内；无法重放或日志对账失败时停止，先修可靠性，不发布策略通过结论。

固定以下小试选择标准：

1. 主比较为 T0 减 B1 的全部家族目标达成率。至少提高 5 个百分点，即 40 个家族中净增加至少 2 个达标家族。
2. 另报共同支持范围，T0 的目标达标数不能低于 B1；覆盖改善与策略改善分别描述。不删除任何不支持家族来改善主结果。
3. 80 次执行都须通过完整前缀重放、原生日志审计与无损归档恢复；同一教师源码、规则版本与配置，随机条件配对一致。
4. 按验证成本再次核对 200 家族的 4 小时保守门槛和现有 20 GiB 目录额度，包括恢复临时空间。若超限，停止扩大采样。

报告按家族配对的 10,000 次 percentile bootstrap，随机种子 `20260921`，区间为 2.5% 和 97.5% 分位。小试选型不要求该区间下界大于 0；若区间覆盖 0，明确说明收益方向尚有不确定性，不能声称正式质量门槛通过。只有上述四项均满足才允许考虑 P4 训练数据采集，未满足时保留结果并停止扩展，不调低门槛或改动本轮候选来覆盖失败。

P2/P3 的累计 2 小时计算额度继续沿用，校准失败、开发尝试及本次验证都计入同一账本，每批最多 30 分钟。登记文件会保存本页、教师、协议、已审计标定报告与原生规则的哈希；已有登记只能核对复用。正式采样、学生训练、LLM 调用和产品建议入口均不由本步骤自动证明可行。
